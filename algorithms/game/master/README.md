# Base game private master

Place the local base-game target tree used to author reconstruction plans here.
The tree defines the relative folders and files a plan must reconstruct, but its
payloads remain private and ignored. Generated replay output belongs in the
shared `algorithms/out/` tree instead.
