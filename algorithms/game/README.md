# Base game algorithm

This family reconstructs the base game. Local evidence belongs in `in/`, the
private authoring target belongs in `master/`, and serialized target plans belong
in `algorithm/`. Generated base-game output is written to the shared `out/`
tree, never below this directory.
