# Muckluck compatibility algorithm

This mod workspace currently expects `in/SHAR Remastered v1_0.lmlm` as its local
compatibility input. Private normalized target content belongs in `master/`,
serialized plans belong in `algorithm/`, and generated mod output belongs below
the shared `algorithms/out/mods/` tree.
