# Muckluck plans

Serialized `.txt` plans for the compatibility mod belong here. Plans are public
only when they satisfy the generic source-bound algorithm and publication
guards; local package or master payloads never belong here.
