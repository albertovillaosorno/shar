# Latin American Spanish language algorithm

This language-mod workspace currently expects
`in/SHAR Versión Latino 0.8.lmlm` as its local compatibility input. Private
normalized target content belongs in `master/`, serialized plans belong in
`algorithm/`, and generated mod output belongs below the shared
`algorithms/out/mods/` tree.
