# Spanish (Spain) language algorithm

This language-mod workspace currently expects `in/dialogs.rcf` as its local
Spanish source evidence. Private normalized target content belongs in `master/`,
serialized plans belong in `algorithm/`, and generated mod output belongs below
the shared `algorithms/out/mods/` tree.
