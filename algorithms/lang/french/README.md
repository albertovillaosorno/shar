# French language algorithm

This language-mod workspace currently expects `in/dialogf.rcf` as its local
French source evidence. Private normalized target content belongs in `master/`,
serialized plans belong in `algorithm/`, and generated mod output belongs below
the shared `algorithms/out/mods/` tree.
