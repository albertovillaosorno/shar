# LMLM imports

Put legacy `.lmlm` files directly in this directory, then run:

```text
cargo run --manifest-path tools/lmlm/Cargo.toml
```

The files here are read-only input and are intentionally ignored by Git.
