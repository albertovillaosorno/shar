---
schema: shar.todo.v1
<!-- markdownlint-disable-next-line MD044 -->
# jig-ignore-next-line: typed TODO identity is canonical
id: preserve-all-64-reviewed-addmission-registrations-across-16-base-demo-and-e3-load
status: completed
priority: P2
lane: unreal
active: false
<!-- markdownlint-disable-next-line MD044 -->
# jig-ignore-next-line: typed TODO identity is canonical
parent: resolve-typed-source-identities-and-intentionally-opaque-values-to-canonical
<!-- markdownlint-disable-next-line MD044 -->
# jig-ignore-next-line: typed TODO identity is canonical
dependencies: [resolve-typed-source-identities-and-intentionally-opaque-values-to-canonical]
children: []
---

# Preserve all 64 reviewed `AddMission` registrations across 16 base, demo, and…

Preserve all 64 reviewed `AddMission` registrations across 16 base, demo, and
E3 load sources in exact authored order. Every declaration has an exact
`<id>i` and `<id>l` sibling and the init sibling selects the same mission id;
registration order remains distinct from unlock, prerequisite, completion, or
progression policy.

## Acceptance criteria

- [x] Preserve all 64 reviewed `AddMission` registrations across 16 base, demo,
  and E3 load sources in exact authored order. Every declaration has an
  exact `<id>i` and `<id>l` sibling and the init sibling selects the same
  mission id; registration order remains distinct from unlock, prerequisite,
  completion, or progression policy.

## Dependencies

Parent task:
`resolve-typed-source-identities-and-intentionally-opaque-values-to-canonical`.

## Evidence

Migrated from a checked canonical TODO item; repository history and
existing technical records remain the implementation evidence.
