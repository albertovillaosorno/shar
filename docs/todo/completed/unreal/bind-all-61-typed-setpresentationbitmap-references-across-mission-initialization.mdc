---
schema: shar.todo.v1
<!-- markdownlint-disable-next-line MD044 -->
# jig-ignore-next-line: typed TODO identity is canonical
id: bind-all-61-typed-setpresentationbitmap-references-across-mission-initialization
status: completed
priority: P2
lane: unreal
active: false
<!-- markdownlint-disable-next-line MD044 -->
# jig-ignore-next-line: typed TODO identity is canonical
parent: resolve-typed-source-identities-and-intentionally-opaque-values-to-canonical
<!-- markdownlint-disable-next-line MD044 -->
# jig-ignore-next-line: typed TODO identity is canonical
dependencies: [resolve-typed-source-identities-and-intentionally-opaque-values-to-canonical]
children: []
---

<!-- markdownlint-disable-next-line MD044 -->
<!-- jig-ignore-next-line: typed TODO heading preserves canonical title -->
# Bind all 61 typed `SetPresentationBitmap` references across mission initialization,…

Bind all 61 typed `SetPresentationBitmap` references across mission
initialization, stage, and objective scopes through one shared canonical P3D
package catalog. The corpus has 56 unique presentation paths, with zero
missing package bindings and zero normalized-root collisions; presentation
timing and drawable semantics remain unresolved rather than inferred from the
package path.

## Acceptance criteria

- [x] Bind all 61 typed `SetPresentationBitmap` references across mission
  initialization, stage, and objective scopes through one shared canonical
  P3D package catalog. The corpus has 56 unique presentation paths, with
  zero missing package bindings and zero normalized-root collisions;
  presentation timing and drawable semantics remain unresolved rather than
  inferred from the package path.

## Dependencies

Parent task:
`resolve-typed-source-identities-and-intentionally-opaque-values-to-canonical`.

## Evidence

Migrated from a checked canonical TODO item; repository history and
existing technical records remain the implementation evidence.
