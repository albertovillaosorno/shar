---
schema: shar.todo.v1
id: bind-all-38-reviewed-setcompletiondialog-ids-to-one-canonical-same-level
status: completed
priority: P2
lane: unreal
active: false
<!-- markdownlint-disable-next-line MD044 -->
# jig-ignore-next-line: typed TODO identity is canonical
parent: resolve-typed-source-identities-and-intentionally-opaque-values-to-canonical
<!-- markdownlint-disable-next-line MD044 -->
# jig-ignore-next-line: typed TODO identity is canonical
dependencies: [resolve-typed-source-identities-and-intentionally-opaque-values-to-canonical]
children: []
---

# Bind all 38 reviewed `SetCompletionDialog` ids to one canonical same-level…

Bind all 38 reviewed `SetCompletionDialog` ids to one canonical same-level
mission-conversation group. Preserve every participant audio package in the
group: 26 groups contain one package and 12 contain two. Resolve all 16
optional character identities independently; do not reinterpret that character
as a speaker or filter audio packages by it. The corpus includes one
`convinit` group and 37 `noboxconv` groups, so conversation mode remains
authored data.

## Acceptance criteria

- [x] Bind all 38 reviewed `SetCompletionDialog` ids to one canonical same-level
  mission-conversation group. Preserve every participant audio package in
  the group: 26 groups contain one package and 12 contain two. Resolve all
  16 optional character identities independently; do not reinterpret that
  character as a speaker or filter audio packages by it. The corpus includes
  one `convinit` group and 37 `noboxconv` groups, so conversation mode
  remains authored data.

## Dependencies

Parent task:
`resolve-typed-source-identities-and-intentionally-opaque-values-to-canonical`.

## Evidence

Migrated from a checked canonical TODO item; repository history and
existing technical records remain the implementation evidence.
