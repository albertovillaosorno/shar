---
schema: shar.todo.v1
id: compile-all-611-direct-objective-and-408-direct-condition-parameter-shapes-into-typed
status: completed
priority: P2
lane: unreal
active: false
parent: map-every-reviewed-participant-route-timing-load-checkpoint-presentation-reward
dependencies: [map-every-reviewed-participant-route-timing-load-checkpoint-presentation-reward]
children: []
---

# Compile all 611 direct objective and 408 direct condition parameter shapes into typed…

Compile all 611 direct objective and 408 direct condition parameter shapes
into typed evidence; preserve the one noncanonical `niether` route token and
undocumented condition values without repairing them. Carry those typed
parameters through semantic projection and the private definition-core join.
The reviewed corpus contains 131 objective invocations and 11 condition
invocations with nonempty direct parameters; no retained parameter is promoted
to runtime objective/failure policy.

## Acceptance criteria

- [x] Compile all 611 direct objective and 408 direct condition parameter shapes
  into typed evidence; preserve the one noncanonical `niether` route token
  and undocumented condition values without repairing them. Carry those
  typed parameters through semantic projection and the private
  definition-core join. The reviewed corpus contains 131 objective
  invocations and 11 condition invocations with nonempty direct parameters;
  no retained parameter is promoted to runtime objective/failure policy.

## Dependencies

Parent task: `map-every-reviewed-participant-route-timing-load-checkpoint-presentation-reward`.

## Evidence

Migrated from a checked canonical TODO item; repository history and
existing technical records remain the implementation evidence.
