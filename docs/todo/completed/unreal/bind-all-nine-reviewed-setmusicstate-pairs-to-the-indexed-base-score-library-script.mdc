---
schema: shar.todo.v1
id: bind-all-nine-reviewed-setmusicstate-pairs-to-the-indexed-base-score-library-script
status: completed
priority: P2
lane: unreal
active: false
parent: resolve-typed-source-identities-and-intentionally-opaque-values-to-canonical
dependencies: [resolve-typed-source-identities-and-intentionally-opaque-values-to-canonical]
children: []
---

# Bind all nine reviewed `SetMusicState` pairs to the indexed base…

Bind all nine reviewed `SetMusicState` pairs to the indexed base score-library
script for their exact source level. Preserve the compiled metadata member
id/path plus the unique named-asset offsets for each `MissionN`/`StageN`
source window. Do not decode RADMusic state-machine or playback semantics from
symbol adjacency.

## Acceptance criteria

- [x] Bind all nine reviewed `SetMusicState` pairs to the indexed base
  score-library script for their exact source level. Preserve the compiled
  metadata member id/path plus the unique named-asset offsets for each
  `MissionN`/`StageN` source window. Do not decode RADMusic state-machine or
  playback semantics from symbol adjacency.

## Dependencies

Parent task: `resolve-typed-source-identities-and-intentionally-opaque-values-to-canonical`.

## Evidence

Migrated from a checked canonical TODO item; repository history and
existing technical records remain the implementation evidence.
