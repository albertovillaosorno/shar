---
schema: shar.todo.v1
id: recreate-materials-only-as-required-to-match-the-original-presentation
status: open
priority: P1
lane: unreal
active: false
parent: null
dependencies: []
children: []
---

# Recreate materials only as required to match the original presentation

Recreate materials only as required to match the original presentation.

## Acceptance criteria

- [ ] Recreate materials only as required to match the original presentation.

## Dependencies

No explicit task dependency was recorded in the legacy TODO.

## Evidence

Open work migrated from the canonical TODO. Add implementation and
validation evidence here as the task advances.

September 5 world-preview investigation isolated apparent transparent walls and
missing ground as presentation defects rather than evidence of deleted source
geometry. The original `RenderFlow::DoAllRegistration` sets PDDI culling to
`PddiCullNone`; the OpenGL PDDI backend implements that state by disabling
`GL_CULL_FACE`. Pure3D's simple-material `2SID` parameter can temporarily
disable culling when it was enabled, but ordinary world rendering already starts
from the no-cull state.

The `l1z1` Simpson-house shaders are opaque (`has_translucency=0`, `BLMD=0`)
and not individually two-sided (`2SID=0`), so their apparent transparency in the
native preview is not source-authored translucency. The preview importer also
intentionally disables FBX material and texture import, leaving Unreal's
one-sided default material in the slots. The world catalog now records the
source-backed regular-scene no-cull contract so native world material families
can reproduce it without changing topology. This additive contract advances the
next generated world catalog to `shar.world-package-collection.v7`; the accepted
historical `v6` artifact remains valid evidence for its earlier topology scope.

Ground geometry is present in the source package. Grass, road, sidewalk,
driveway, and asphalt material groups contribute 1,324 triangles in `l1z1`.
The Unreal-target topology sidecar omits only 13 source triangles across the
entire zone (11 zero-area and two repeated-index), with just one zero-area
omission in the sampled grass track. Do not add a replacement
floor or duplicate/backflip faces to compensate for the current preview.

September 5 material-projection follow-up makes the next world publication
self-describing for native presentation. `shar.world-package-collection.v8`
publishes base `material_bindings` with independent `binding_sha256` and
`presentation_sha256` identities, normalized PNG identity, decoded diffuse RGBA8
tint, and six overlapping source surface-semantic flags.

The same artifact now publishes exact binary-writer `material_slots`. A base
binding may fan out when geometry identity adds effective semantics, so each
slot records its exact `slot_name`, `source_material_name`, base hashes, and
an effective `slot_presentation_sha256` with its effective semantic flags.

Pure3D shader and texture animation controllers resolve their target shader by
logical identity, so distinct source shader identities remain independent even
when presentation hashes match. Unreal automation can address the exact writer
slot and join back through `source_material_name`; Material Instances may reuse
an effective presentation hash only without merging those runtime targets.

Full-source v8 acceptance publishes 190 FBX artifacts with 7,127 base binding
occurrences and 7,215 exact writer slots. The 88 additional slots explain all
31 earlier binding-to-material count gaps, while FBX bytes, base bindings, and
the 1,756 root textures remain byte-for-byte deterministic. The exhaustive
catalog audit reports zero errors.
