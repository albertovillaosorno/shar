---
schema: shar.todo.v1
id: preserve-source-topology-uvs-materials-textures-pivots-rigs-animations
status: open
priority: P0
lane: conversion
active: false
parent: null
dependencies: []
children: []
---

# Preserve source topology, UVs, materials, textures, pivots, rigs, animations,…

Preserve source topology, UVs, materials, textures, pivots, rigs, animations,
placements, and transforms without artistic edits.

## Acceptance criteria

- [ ] Preserve source topology, UVs, materials, textures, pivots, rigs,
  animations, placements, and transforms without artistic edits.

## Dependencies

No explicit task dependency was recorded in the legacy TODO.

## Evidence

World-analysis mesh loading now uses the same strict authored-topology contract
as ordinary FBX mesh loading. Triangle-strip stitching degenerates remain a
deterministic topology conversion performed by strip triangulation, while an
authored triangle-list record with repeated vertex indices fails closed instead
of being silently deleted. A focused regression proves analysis no longer
repairs a malformed triangle list; the complete FBX package, strict FBX Clippy,
and all 54 world-level tests pass. The legacy world-catalog discard-count field
is retained for schema compatibility and is now zero for render meshes.

Intersect collision meshes now follow the same fail-closed rule. Their decoded
format is an explicit count-prefixed triangle index array with no strip/stitch
selector, so repeated-index source triangles are no longer deleted before mesh
construction. A focused collision regression proves the exact malformed source
topology is rejected, and the legacy collision discard count remains zero for
catalog compatibility.

World mesh publication now also preserves source mesh grouping itself. Spatial
distance between disconnected components no longer triggers vertex compaction,
primitive regrouping, or generated independent-object identities. Near and
distant synthetic meshes remain byte-equivalent at the typed mesh boundary,
and the position/UV preservation regression remains green.

Rigid-prop rest-transform baking now applies the inverse-transpose affine basis
to authored normals instead of reusing the position basis. Non-uniform authored
joint scale therefore preserves surface orientation while positions still use
the exact source rest transform; a focused regression proves a diagonal normal
under 2x X scale resolves to the mathematically correct transformed direction.

Skeletal PTRN binding now fails closed when a bound source group carries a
transform property other than the explicitly preserved `TRAN` and `ROT_`
channels. Unsupported authored transform data can no longer disappear from the
export silently; unmatched helper groups remain retained as ignored evidence.

Skeletal animation rest defaults now decompose the authored local matrix before
quaternion recovery, removing scale from the rotation basis while retaining the
exact decomposed translation. A 90-degree Z rest rotation with 2x authored X
scale now recovers the original rotation instead of a scale-distorted angle.

Bound skeletal animation keys now must remain inside the declared clip frame
range. Authored keys beyond the last exported frame fail closed instead of being
silently truncated while interpolation samples only the shorter clip duration.

Skeletal PTRN loading now also models decoder-produced top-level loose channels
and legacy animation extras explicitly. Non-empty payloads fail closed instead
of being silently discarded by Serde while grouped skeletal tracks are exported.

Skeleton loading now rejects joint-control semantics that the current FBX bone
contract cannot represent: nonzero DOF/free-axis/axis selectors and decoded
joint metadata. These authored rig controls can no longer be flattened silently
into an unconstrained parent/child hierarchy.

Whole-character loading now retains decoded composite-effect presence through
the shared binding parser and rejects non-empty effects because the current FBX
character contract cannot preserve their joint/translucency/sort semantics.
Explicit selected-rigid-prop export may still prune unrelated effect branches.

Primitive-group normal attachment now preserves already validated authored
triangle winding instead of swapping the second and third index when geometric
facing disagrees with the summed source vertex normals. Source topology and
source normals therefore remain independent evidence through the FBX domain;
the declared FBX export-root transform remains the orientation conversion
boundary.

A normalized-corpus audit found the removed repair would have changed 327 of
190,162 orientable mesh triangles across 64 primitive groups and 2,741 of
268,870 orientable skin triangles across 211 groups. These are input-evidence
counts, not a claim that every audited component is published. The complete FBX
package and all 1,006 pipeline library tests pass with strict FBX and pipeline
Clippy clean.

Decoded P3D triangle-list groups now require exact triples instead of allowing a
four-index partial list to fall through the generic quad triangulation helper.
Both mesh and skin adapters reject that malformed source before mesh or skin
binding continues, while explicit non-P3D quad callers retain their separate
domain helper contract. The normalized corpus contains 3,055 mesh and 198 skin
`prim_type=0` groups; every one has an index count divisible by three. Focused
red/green regressions cover both adapters and the full FBX and pipeline suites
remain green.

Decoded mesh and skin UV intake now has focused evidence for the existing
fail-closed single-channel contract. Both adapters reject a populated nonzero UV
channel instead of choosing one channel and dropping the rest; they also reject
duplicate channel-zero evidence. The normalized corpus contains 18,148 mesh
primitive groups and 1,462 skin primitive groups. All 19,610 groups carry
exactly one non-empty channel zero, with no nonzero or duplicate channel
identity observed.

Decoded P3D mesh `RENDERSTATUS` is now interpreted according to the schema's
`CastShadow` field instead of being discarded. `MeshAsset` retains the optional
boolean source flag, binary FBX mesh nodes publish declared values as
`SHAR_P3D_CastShadow` source metadata. Values outside `0` or `1` fail
closed.

The normalized corpus contains 14,297 decoded meshes: 14,283 declare `1`, 12
declare `0`, and two omit the chunk. A focused binary regression verifies the
disabled value is encoded as `0`, not merely that the property name exists.

Packed-normal evidence is redundant rather than exclusive in the current
normalized corpus. All 3,642 mesh groups and 1,462 skin groups carrying packed
normals also carry same-length decoded ordinary-normal arrays; no packed-only
group was observed. Across the same 19,610 primitive groups, no decoded tangent,
binormal, or multi-colour field is present. The adapters' strict JSON shape
continues to reject unsupported fields rather than dropping them silently.

A complete raw-game locator audit covers 6,580 SRR locator chunks with zero
`NumTriggers` mismatches against physical trigger-volume children. All 69
occlusion locators carry exactly one authored data word, so extraction now
rejects a missing word instead of synthesizing an occlusion count from the
trigger total.

The same raw audit finds 1,866 schema-sized `EXTRA_MATRIX` children, all finite,
with 1,571 distinct matrix payloads; fresh locator JSON now retains those
matrices without assigning gameplay meaning. It also finds 129 locator splines
with four through 13 finite control points, and every spline carries exactly one
nested rail record. Fresh extraction retains those spline and rail fields as
source evidence rather than dropping the child structure or interpreting camera
behavior.

Trigger-volume authority is also complete for the current raw corpus. All 3,947
trigger-volume chunks are childless exact-header records; 1,213 use type 0 and
2,734 use type 1, with no other type values. Every authored scale and matrix
float is finite. Fresh extraction now rejects unobserved types, non-finite
values, trailing header bytes, and child payloads rather than normalizing them.

Road topology recovery now retains the physical road-segment layer that was
previously discarded. The corpus contains 758 roads with 6,768 direct
road-segment children, 6,507 road-segment-data records, and 443 intersections.
Every header is exact, every road child is a road segment, every road segment is
childless, and all segment matrices plus segment-data and intersection geometry
are finite. Recovered roads preserve each segment name, segment-data reference,
hierarchy matrix, and scale matrix in source order.

Package-local reference evidence has an important asymmetry. All 6,768 authored
road-segment-data references resolve to exactly one physical segment-data record
in the same package. Road endpoint intersection tokens are not
occurrence-unique:
1,080 endpoint references match one physical intersection, 427 match two, and 9
match three. No first, nearest, or ordinal-based intersection occurrence is
selected from those duplicate logical names.

Adjacent world-network evidence is equally bounded. The corpus contains 605
pedestrian paths with five through 23 finite authored points; every path is
childless and exact-header, and no `PED_PATH_SEGMENT` chunk is observed. It also
contains 175 exact 40-byte follow-camera records with finite camera values;
their
widely varying IDs remain verbatim source data rather than an inferred enum.

The one attribute table has 63 exact childless rows and 189 finite physical
property floats. Values reach 9999, so recovery preserves those authored
magnitudes rather than imposing guessed physics ranges. All nine lens flares are
version 0: seven declare and contain three billboard quad groups and two declare
and contain four, followed in every case by exactly one mesh and one composite
drawable.

All 207 breakable objects have exact 20-byte headers and only schema-declared
direct child families. `BreakableType` and `MaxInstances` remain uninterpreted
because no child-count equation is source-proven. The 16 chunk sets are all
version 0; their declared counts exactly cover 257 direct children, every one a
texture. The decoder therefore no longer widens chunk-set membership to mesh or
skin children that are absent from both schema and current source.

Deferred world billboard controller relationships now retain their exact BQG
animation member identity and source ordinal instead of losing the animation
edge when model FBX publication omits billboard runtime semantics. This is
relationship preservation only: the decoded BQG channel payload remains the
source authority and is not flattened into skeletal or static FBX animation.

A focused audit of the 138 linked BQG animations finds 134 at 30 fps and four at
24 fps; all are non-cyclic and have no loose channels or legacy animation
extras. Eight animations contain four named groups, while the other 130 contain
one group. Channel sequences split into 82 standard eight-channel records, 43
width/height-only records, eight four-group repetitions of the standard set,
and five extended records that also carry `ERNG`, `SRNG`, and `ORNG`. Those
source differences remain explicitly deferred for a later faithful billboard
animation representation rather than normalized away.

Deferred world billboards now preserve their source presentation payload
separately from the static inspection planes used by FBX review. Across the 284
current child quads, authored orientation modes are 254 `NOAX`, 19 `YAX`, eight
`AAX`, and three `LYAX`; cutoff modes are 279 `N0NE` and five `DBL`. All current
children enable perspective, while authored distance is zero for 276 children,
`0.65` for seven, and `-0.35` for one.

A complete raw-game P3D audit now covers 2,432 billboard quad groups and 3,195
quad children. Every group declares exactly its physical child count. Every
group uses schema version 0, every quad uses version 2, and every quad carries
one display-info child followed by one perspective-info child.

Perspective-info is version 0 for all 3,195 children. Of the display-info
children, 2,944 use version 0 and 251 use version 1, so discarding that version
erased real source evidence. Fresh extraction now retains both optional child
versions, and deferred world provenance carries them without assigning runtime
meaning.

The current UV-offset range, source range, and edge range are zero for all 284
children, but the v4 catalog still retains those fields rather than inferring a
global default. Ten shader identities and 55 child identities occur across the
260 groups, and no audited presentation float is non-finite. Camera-facing,
cutoff, depth, and BQG animation execution remain deferred to a reviewed native
presentation adapter instead of being flattened into static or skeletal FBX.

Deferred billboard shader evidence now uses the same validated decoded-shader
contract as FBX material resolution while preserving the full source parameter
array. Schema/version, platform shader identity, translucency, vertex masks,
declared parameter count, canonical `TEX`, and exact decoded parameter values
remain available without translating them into runtime material semantics.

Across the current 260 deferred groups, the retained relationships reference ten
shader identities and nine canonical texture tokens. Their 271 shader
occurrences carry 7,017 parameter rows through the catalog boundary; nine of the
11 ambiguous relationship sets differ in non-texture shader payload while none
disagree on `TEX`. Native blend, depth, camera-facing, cutoff, and animation
execution therefore remain deferred instead of being inferred from one selected
shader occurrence.

Replaying the existing cross-package texture authority against the phase-three
index resolves 248 of the 260 deferred billboard texture references without
conflict. The remaining 12 are all level-03 `glow2.bmp` uses: their preferred
terrain scope contains two distinct normalized PNG payloads for the same source
identity. No local billboard shader candidate disagrees about the `TEX` token,
but that agreement does not identify one of the two physical texture payloads.

Both conflicting level-03 textures are independent top-level chunks, and the
terrain shader that names `glow2.bmp` is also an independent top-level chunk.
There is therefore no decoded parent/container edge supporting an occurrence
choice. Texture publication for those deferred billboard semantics remains open
until stronger source or native evidence can disambiguate the relationship.

Deferred billboard provenance now retains the physical texture side of that
open relationship as well as the logical shader token. The same existing scope
preference used for material staging contributes every preferred package/member
occurrence, its source ordinal, and normalized PNG digest to
`shar.world-model-props.v6`; no preferred occurrence is promoted to an authored
relationship by this step.

The 260 current logical billboard texture references retain 300 physical
occurrences. Twenty-two references have multiple preferred occurrences whose
payloads are byte-identical, while the 12 level-03 `glow2.bmp` references retain
their two differing payloads as explicit ambiguity. Runtime texture selection,
material recreation, and native billboard presentation remain deferred rather
than being inferred from this provenance inventory.

The 138 controller-linked BQG clips were rechecked at the decoded channel
boundary. All declared group, channel, and keyed-frame counts agree with their
payloads; keyed frames are strictly increasing and within the declared clip,
and all 934 observed interpolation records use mode 1. Every one of the 138
animation group sequences exactly matches its linked billboard child sequence.

Across those 162 linked animation-group/child pairs, every frame-zero width,
height, distance, UV offset, translation, colour, edge-range, source-range, and
offset-range channel value matches the corresponding decoded static child field
when a frame-zero key exists. However, 49 width/height channels and 38 colour
channels have no frame-zero key, and rotation differs from the static rotation
in 75 of 119 frame-zero cases. That mixed evidence rules out flattening BQG
channel data into static billboard defaults without a reviewed runtime
interpretation.

A dedicated decoded billboard-animation evidence adapter now validates BQG
source payloads independently from skeletal FBX animation. It preserves source
frame span and rate, cyclic state, platform-size records, authored group order,
group ids, declared counts, six observed channel representation families, key
frames, values, and interpolation metadata without sampling or flattening them.
Unsupported top-level payloads, channel semantics, contradictory
counts,
out-of-range or unordered keys, and non-finite floating values fail closed.

The exact deferred-controller relationship set was replayed through that reader:
all 138 linked animations pass, retaining 162 groups and 1,053 channels. World
intake also requires the validated animation-group sequence to equal the linked
billboard-child sequence before provenance is accepted. Their validated payloads
travel with controller provenance in `shar.world-model-props.v7`; this remains
source evidence only and does not claim native camera-facing, cutoff, material,
or BQG runtime execution.

The reader was then replayed against all 1,100 normalized BQG animations. The
corpus contains one platform-size record and one group list per clip; all 8,158
non-visibility channel metadata records use interpolation mode 1. Rotation uses
636 ordinary quaternion channels and 437 packed-quaternion channels, while all
1,108 visibility channels carry a `u16` start state plus ordered raw `u16`
values rather than a decoded boolean-value vector.

The corrected reader accepts 1,098 clips, retaining 1,212 groups and 9,247
channels. Two clips remain rejected because the P3D decoder deliberately renders
non-finite source floats as JSON `null`: one rotation key and one width
sequence. No numeric value is synthesized for those cases. World provenance
advances to
`shar.world-model-props.v8` so visibility is serialized as `raw_values` and
packed rotations remain distinct compressed source evidence.

Vehicle BQG evidence has the same source relationship structure even though its
runtime presentation remains deferred. The current vehicle corpus contains 65
BQG clips across 21 packages; every clip has one controller, one matching quad
group, a composite reference, and exact animation-group-to-billboard-child
ordering. The vehicle catalog now retains those source identities and ordinals
alongside the verbatim effect-animation sidecar.

One vehicle BQG sidecar contains the already-audited non-finite width sequence.
It remains preserved as decoded JSON with `null` values and is not passed to the
skeletal animation reader or repaired by the new relationship metadata. Native
billboard animation semantics therefore remain open rather than being inferred
from the sidecar relationship.

Vehicle texture-effect provenance is now separated from billboard animation
rather than falling through as an untyped sidecar. The five current `TEX_`
animations each have one `TEX` frame controller whose hierarchy resolves
uniquely to a shader; all five decoded animation payloads contain one entity
channel with parameter `TEX_` and no non-finite JSON values.

The vehicle catalog retains that controller-to-shader source relationship and
its exact source ordinals, but the effect JSON remains verbatim. Texture-frame
selection and shader runtime animation remain deferred because this step does
not interpret the entity-channel payload or recreate native material behavior.

Vehicle intake now validates that limited source envelope before publishing a
`TEX_` sidecar. Across all five clips, declared entity-key counts equal
frame and value counts, frames are strictly increasing and inside the clip
span, group identity exactly matches `TEX_` plus the controller-target shader,
and all five interpolation metadata records are version 0/mode 1. The 48 entity
values are
validated only as fixed-width source identities; their runtime texture meaning
remains deliberately uninterpreted.

Those 48 entity references resolve to 12 logical texture identities in their
owning vehicle packages. Sixteen references have one exact physical texture
occurrence, while 32 references have two exact same-name occurrences. Of those
32 ambiguous references, 23 candidate pairs are byte-identical and nine contain
two different normalized PNG payloads.

None of the 32 ambiguous references has a candidate sharing the animation,
controller, or target-shader container. Container ownership therefore cannot
select a physical texture occurrence, and ordinal proximity would be an invented
relationship. The entity channel remains logical source evidence until stronger
source or native semantics can disambiguate those nine differing-payload cases.

The target shader does establish the logical frame-zero state. Each of the five
current target shaders has exactly one canonical `TEX` parameter, and every one
matches the `TEX_` entity-channel key at frame zero. Vehicle intake validates
that equality against the exact target shader source ordinal before preserving
the sidecar, so shader defaults and animation evidence cannot silently disagree.

This still does not assign runtime texture-frame semantics or choose a
duplicated PNG. Three frame-zero logical identities have duplicate physical
occurrences; one pair is byte-identical and two pairs differ in payload. Logical
initial-state agreement is therefore stronger provenance, not physical
occurrence authority.

Vehicle catalog v4 now preserves every package-local physical occurrence behind
those logical `TEX_` entity values without assigning runtime meaning. Across the
five clips, 32 first-use logical reference records retain 48 normalized PNG
occurrences; half the reference records have one occurrence and half have two.
Nine duplicated reference records contain differing payload digests, so all
candidates remain visible rather than being collapsed to one texture frame.

Occurrence provenance comes from the normalized component ledger that directly
addresses each hashed PNG: physical member id, source component ordinal, and
SHA-256 are retained together. The phase-three package index preserves the same
physical coordinate rather than introducing another ordinal domain. All 1,479
current car texture members match one ledger PNG by exact path and carry the
same component ordinal.

That one-to-one mirror is occurrence identity, not relationship authority. A
shader still exposes only its logical texture string, so the phase-three member
cannot select between two same-name physical occurrences without another source
edge.

The ambiguity is active on the current vehicle render path rather than only
sidecar debt. All 11 quad groups using the five `TEX_` target shaders pass the
strict billboard-source contract and are composite-reachable, so their shaders
participate in retained material resolution. Three frame-zero logical texture
identities have duplicate local ledger occurrences; the material adapter rejects
all three instead of selecting one, including the byte-identical duplicate pair.

Two of those duplicate pairs contain different PNG payloads, so weakening that
fail-closed behavior would invent source semantics. End-to-end publication for
those three target materials therefore remains blocked pending stronger source
or native occurrence authority even though v4 can represent every candidate.

The vehicle-wide texture audit is larger than the five `TEX_` effects. Across
786 shader texture bindings that are actually referenced by retained mesh or
billboard geometry, 402 bindings have two same-package normalized texture
occurrences. Those duplicates span 54 car packages; 286 pairs are
byte-equivalent and 116 pairs contain different PNG payloads.

Vehicle texture authority therefore now derives logical identity from each
normalized texture ledger row while retaining its exact physical PNG path,
member id, source component ordinal, owning package evidence, and SHA-256. The
current 89 car ledgers provide complete one-to-one coverage for all 1,479
normalized PNGs, with no path or source-ordinal gaps.

This matters for collision-renamed payloads: 461 texture rows have physical file
names whose normalized lookup key differs from the authored ledger identity,
and 420 of those paths carry an explicit collision suffix. Authority lookup now
groups those physical occurrences by authored identity before applying the same
payload-equivalence rule; it does not infer which differing occurrence a shader
meant.

The remaining 116 differing-payload used bindings therefore stay fail-closed.
The new occurrence evidence improves diagnostics and future sidecar authority,
but does not turn source ordinal, filename, or byte equivalence into unsupported
runtime texture-selection semantics.

A current-route replay found zero used shader bindings whose selected material
package lacks the logical texture and therefore needs cross-package fallback.
The ledger-key correction is still required for that authority boundary, but it
does not bypass the 402 local duplicate relationships that fail before fallback
is considered.

All 402 duplicate used bindings share the same top-level layout: two self-owned
texture chunks with parent ordinal 0 precede a self-owned shader chunk with
parent ordinal 0, and exactly one texture path receives the extractor's
generated ordinal collision suffix. The later texture is always nearer the
shader, but the source shader texture-parameter schema stores only `Param` plus
a string `Value`.
It has no occurrence ordinal or handle, so proximity cannot safely select one
physical payload.

The FBX material adapter now exposes a separate exact texture-occurrence
boundary for consumers that later obtain real relationship authority. A caller
may supply one exact package-ledger PNG occurrence together with an exact shader
path; the physical path selects the occurrence, while the shader's authored
`TEX` identity controls the staged PNG name. Unpublished files and shaders with
no texture parameter are rejected. Ordinary material resolution is unchanged
and still rejects ambiguous same-name ledger candidates.

Community tooling evidence narrows ordering semantics without resolving that
ambiguity. Donut Team guidance for Lucas' Pure3D Editor requires a texture chunk
to appear above the shader that references it and describes shared textures as
resources found by the shader's logical texture name. All 402 current duplicate
vehicle bindings already have both same-name texture occurrences above their
shader. The public ordering evidence therefore cannot distinguish the base path
from the ordinal-qualified collision path.

`shar.vehicle-catalog.v5` now carries that verified occurrence coordinate into
TEX sidecar evidence. Every retained physical texture occurrence requires one
unique phase-three `p3d-texture` member whose exact package path and source
chunk ordinal both match the normalized ledger row. The stable package-member id
is
serialized alongside the physical member id, component ordinal, and PNG digest.

This is still non-selecting provenance. Multiple same-name occurrences remain
separate v5 records. Neither the phase-three id nor its ordinal is treated as a
shader-to-texture relationship. The new exact-occurrence FBX boundary can use
one physical member only after another source-backed consumer establishes that
relationship authority.

`shar.world-model-props.v9` now gives every retained deferred billboard
texture occurrence the same stable phase-three physical coordinate. A whole
terrain-world audit covers 149 packages and 5,866 normalized texture ledger
rows: every row
matches exactly one phase-three `p3d-texture` member by exact component path,
every member carries the same source chunk ordinal as its ledger row, and there
are zero missing, duplicate-path, or unrepresented occurrences.

World shared-texture authority now requires that exact path-and-ordinal join
while it ingests a ledger occurrence, then retains the phase-three
package-member id
alongside the existing package id, subcategory, normalized member id, source
ordinal, and PNG digest. A focused regression proves that a matching path with a
wrong ordinal, or a matching ordinal with a wrong collision path, fails closed.

This strengthens physical occurrence identity only. The existing scope
preference and payload-equivalence rules are unchanged, so the 12 differing
`glow2.bmp` preferred occurrence sets remain unresolved and all same-name
physical peers stay visible rather than being promoted to an authored shader
relationship.

`shar.world-model-props.v10` extends the same exact physical coordinate to
deferred billboard shader occurrences. A separate whole terrain-world audit
covers 9,991 normalized shader ledger rows across the same 149 packages: every
row matches exactly one phase-three `p3d-shader` member by component path and
source ordinal, with zero missing, duplicate-path, or unrepresented rows.

World inventory now builds a package-local phase-three shader map keyed only by
that exact path-and-ordinal pair. Every same-name shader occurrence retained for
a deferred billboard must resolve through the map, and v10 serializes the stable
package-member id beside the normalized shader member id and decoded authored
shader payload. The existing same-name occurrence list remains
source-ordinal ordered and non-selecting; this adds provenance without choosing
a shader winner.

The same audit method identifies the next bounded world provenance gap without
changing v10 behavior. Across the 149 terrain-world packages, all 1,091
`quad_group` rows match `p3d-mesh` phase-three members by exact path and
ordinal,
all 4,032 `animation` rows match `p3d-animation` members, and all 2,802
frame-controller-family rows match `p3d-controller` members. The controller set
contains 2,796 `frame_controller`, four `frame_controller_variant_a`, and two
`frame_controller_variant_b` rows; every family has zero missing,
duplicate-path, ordinal-mismatch, or unrepresented rows.

Those coordinates are not yet published by v10 for deferred quad-group,
controller, or animation relationships. Their current normalized member ids and
source ordinals remain truthful, so this is a stable-identity provenance gap,
not permission to infer new controller, animation, or presentation semantics.

Phase-three physical coordinates are now an intake invariant rather than an
adapter convention. A whole generated-index audit covers 161,960 current
physical members: member paths are unique within every package, and all 132,179
members that carry a source chunk ordinal also have a package-unique ordinal.
Across the 12 package categories with normalized component ledgers, all 132,179
path-bearing ledger rows map one-to-one to those same phase-three members by
exact published path plus source ordinal, with zero missing or ambiguous rows.

`PhaseThreePackageRow` now rejects duplicate physical member paths and duplicate
non-null source ordinals before any conversion adapter can observe the package.
It also exposes one exact path-and-ordinal lookup. Vehicle and world texture
occurrence joins use that shared boundary, while the world shader coordinate map
can rely on intake uniqueness instead of maintaining a second ambiguity guard.
This centralizes physical occurrence identity only; it does not turn source
ordinal into logical shader, texture, controller, or render relationship
authority.

`shar.world-model-props.v11` closes that deferred world provenance gap using
the centralized phase-three coordinate invariant. Terrain-world intake now
builds one classified source-member table for every source-ordinal package
member. Deferred composite quad groups must resolve as `p3d-mesh` model members,
frame-controller-family relationships as `p3d-controller` controller members,
and retained BQG animations as `p3d-animation` animation members at the exact
ledger path and source ordinal.

The catalog now serializes those stable phase-three ids beside the existing
normalized member ids and source ordinals. This covers the previously audited
1,091 quad groups, 2,802 frame-controller-family rows, and 4,032 animations
without changing composite selection, controller-to-animation matching, BQG
validation, or presentation semantics. A classification mismatch fails closed;
phase-three identity still records a physical occurrence rather than inventing
a logical relationship.

The next bounded world provenance gap is the primary model route rather than the
deferred render path. A route-level audit mirrors the current world inventory
across 840 model-bearing terrain-world candidates. It finds 370 rigid-animated
candidates, 55 composite-backed static candidates, and 415 static candidates
without a matching composite. Composite selection retains 2,368 mesh
occurrences in total; every selected mesh resolves to an exact phase-three
`p3d-mesh` model member at its ledger path and source ordinal.

All 425 matching composite occurrences likewise resolve as phase-three
`p3d-composite-drawable` model members. Their authored skeleton relationships
resolve to 425 exact `p3d-skeleton` animation members. Of those candidates, 370
also have the exact same-container `PTRN_<skeleton>` relationship used by the
rigid-animated route, and all 370 clips resolve as `p3d-animation` animation
members. The remaining 55 composites have the authored skeleton but no exact
PTRN clip, so the current route remains static and does not consume that
skeleton.

The world catalog source alias currently publishes none of those primary
phase-three member ids; it retains only package/container identity plus deferred
render evidence. A future catalog revision can add selected mesh member ids in
the existing authored selection order plus separately labelled
composite/skeleton/PTRN provenance without
changing semantic deduplication or implying that the 55 static skeletons were
exported. This is a physical-provenance gap only; current mesh selection and
animation routing remain source-backed.

That selected-mesh ordering must not be relabelled as numeric source-ordinal
order. Among the 425 composite-backed candidates, 233 project mesh occurrences
in an authored composite-prop order that differs from sorting those same meshes
by ledger ordinal. The remaining 415 non-composite static candidates continue to
use ledger source-ordinal order. Primary provenance must preserve whichever
source-backed ordering the existing route actually uses.

`shar.world-model-props.v12` closes the audited primary world
physical-provenance
gap without changing route selection or semantic deduplication. Every world
source alias now retains the exact phase-three package-member id, normalized
member id, and source ordinal for its owning container plus every selected mesh
occurrence. The mesh records stay in the order actually consumed by the current
route: `source_ordinal` for the 415 non-composite static candidates and
`composite_prop` for all 425 composite-backed candidates, including the 233
whose composite order differs from numeric ledger order.

Matched composite, referenced skeleton, and exported PTRN animation provenance
are represented as separate optional records. This preserves all 425 matched
composites and all 425 authored skeleton relationships, while only the 370
rigid-animated candidates carry an `exported_ptrn_animation`. The 55 static
composite-backed cases therefore retain their real skeleton provenance without
claiming that the skeleton or a nonexistent PTRN payload was exported. All 55
have a frame controller naming `PTRN_<skeleton>`, but a package-wide audit finds
no matching decoded PTRN animation payload in the same or another container, so
controller naming remains non-authoritative for route promotion.

The expected full-corpus v12 primary counts are 840 source bindings, 2,368
selected meshes, 425 matched composites, 425 referenced skeletons, and 370
exported PTRN animations. Owner occurrences are classified through the same
phase-three taxonomy as package generation: dynamic and instanced-dynamic DSG
owners are `physics/p3d-physics`, animated/state-prop factories are
`world/p3d-animated-prop`, and the remaining supported SRR model owners are
`world/p3d-world-dsg`. Classification mismatches fail closed.

The next bounded composite relationship gap is authored prop presentation rather
than physical member identity. Across the 425 matched world composites, 2,213
prop rows participate in the current source relationship surface: 1,953 select
mesh members and 260 reference non-selected render identities. All 1,953
selected rows are authored `prop` records with valid binary translucency and
joint ids from 0 through 58; 755 are translucent and 1,198 are opaque.

`sort_order` is not a corpus-wide constant. Selected mesh bindings contain
1,887 values of 0.5 and 66 values of 0.4; the 66 non-default selected rows are
translucent monkey-gag parts. The 260 non-selected prop rows contain 225 values
of 0.5, 26 of 0.4, eight of 0.49, and one of 0.0. The 0.4 non-selected cases
include chevron and power-box billboard relationships, the 0.49 cases are
`donotenter` billboard relationships, and the 0.0 case is the power-box flash
billboard relationship.

Current rigid FBX assembly consumes selected prop joint and translucency
bindings, while the pipeline's compact composite evidence retains those same
fields for deferred relationships. Both paths currently discard authored
`sort_order`, and v12 primary member provenance does not yet serialize selected
mesh joint/translucency bindings. Preserve those exact source values before any
future rendering-order interpretation; this audit does not establish what
`sort_order` means at runtime.

`shar.world-model-props.v13` now preserves the audited composite prop
relationship evidence without interpreting rendering order. Each of the 1,953
selected composite-backed mesh occurrences retains its authored composite prop
index, skeleton joint id, translucency flag, and optional `sort_order` at exact
source `f32` width beside the v12 physical member identity. The 415
non-composite static selections retain null composite-binding fields rather than
inventing relationships.

Deferred non-mesh render relationships retain the same optional source-width
`sort_order` beside their already-published prop index, joint, and translucency.
On the current 425 matched world composites, all 2,213 selected and deferred
prop rows carry a sort-order child. The expected v13 aggregate counts are
1,953 primary composite mesh bindings and 2,213 retained composite sort-order
values.
The source decoder permits the sort-order child to be absent, and v13 preserves
that absence as null instead of substituting a default.

Current values remain evidence only. In the audited selected mesh set, 1,887
sort values are 0.5 and 66 are 0.4; the deferred set additionally contains 0.49
and 0.0 cases. The catalog does not treat those numbers as depth, priority, draw
order, or any other runtime semantic until source-backed behavior establishes
such a mapping.

The direct rigid-animated FBX prop-level provenance gap is now closed.
`decoded_skin_source::composite_bindings` retains every authored prop position,
skeleton joint, binary translucency flag, and optional source-width sort value.
`CharacterSourceProvenance` carries validated `CompositePropSourceBinding`
records keyed by retained composite ordinal plus authored prop index, so
repeated composite identities and repeated prop names remain unambiguous.

The binary character writer publishes those rows as indexed export-root source
properties for prop identity, skeleton joint, translucency, and exact `f32`
sort-order bits. A genuinely absent optional sort field is represented as
`absent`; a present JSON `null` or other non-finite/non-`f32` value fails closed
instead of collapsing into absence. Current normalized corpus evidence remains
1,518 composites and 7,456 authored prop rows, all with finite sort values.

The relationship key is necessary in current source. Eighty-two composites
repeat prop names, including 80 of 88 selected vehicle composites, where wheel
shape identities deliberately repeat on different joints. The direct world
rigid-animated route covers 370 composites and 2,055 authored prop rows: 1,868
selected mesh relationships plus 187 other authored relationships. Supplemental
vehicle bindings remain excluded because they are assembly inputs rather than
authored composite rows.

This remains provenance-only. `prepared_signature` clears
`asset.source_provenance` before semantic deduplication, so these source records
do not change variant identity or assign runtime meaning to sort values. The
writer therefore preserves evidence such as vehicle 0.1157/0.4/0.45/0.48/0.49
values, Wrench's selected 0.4, and Wasp Camera's selected 0.5 without treating
them as depth, priority, or draw-order semantics.

Composite skin relationship provenance is now preserved alongside prop
provenance in the direct FBX path. Across the normalized corpus, 1,518
composites contain 633 authored skin rows in 208 packages.

Every current row is kind `skin`, resolves to a same-package skin component,
has an explicit binary translucency flag, and carries a finite sort-order
child. The current sort values are 625 occurrences of 0.5 and eight occurrences
of 0.1; 15 rows are authored translucent. No current composite repeats a skin
identity.

`CharacterSourceProvenance` now retains each authored skin row by retained
composite ordinal plus authored skin index, with exact skin identity,
translucency, and optional source-width `f32` sort bits. The binary character
writer publishes those values as indexed export-root source properties. A
missing optional sort child remains `absent`, while present `null`, non-finite,
or non-`f32` values fail closed. The decoder also requires the normalized
binding kind to be exactly `skin`; all 633 current rows satisfy that contract.

These records remain source-only. Existing skin translucency projection is
unchanged, and semantic deduplication still clears `asset.source_provenance`
before hashing. The preserved 0.1/0.5 values therefore gain no inferred runtime
ordering meaning.

The next unresolved composite relationship family is authored effects. The
normalized corpus contains 804 effect rows across 212 packages; all are kind
`effect`, all carry a skeleton joint and finite sort value, and no composite
repeats an effect identity. Current sort values are 777 occurrences of 0.5, 22
of 0.4, two of 0.499, two of 0.1, and one of 0.3. Exactly 803 rows are authored
translucent and one is opaque.

A logical effect name is not physical occurrence authority. For 537 rows, the
same package contains both a same-name `particle_system_factory` and a same-name
`particle_system`; the remaining 267 rows have no exact same-package component
identity match. Therefore neither first-match nor kind guessing can safely turn
an effect row into one physical particle occurrence.

Direct rigid-prop FBX provenance now retains those authored effect rows without
resolving particle ownership. `CompositeEffectSourceBinding` preserves retained
composite ordinal, authored effect index, exact effect identity, skeleton joint,
binary translucency, and optional source-width `f32` sort bits. The binary
character writer publishes the same fields as indexed export-root source
properties. Missing optional sort remains `absent`; present `null`, non-finite,
wrong-kind, out-of-range-joint, or non-binary-translucency evidence fails
closed.

This does not broaden full-character behavior: `load_character` still rejects
any composite with effects through `UnsupportedCompositeEffects`. Rigid-prop
loaders preserve effect evidence because those routes already accepted such
composites; supplemental runtime bindings remain separate from authored rows.
Semantic deduplication still clears the aggregate source provenance before
hashing.

The remaining particle relationship is intentionally unresolved. For all 537
same-name factory/system pairs, the particle-system payload explicitly names the
same-name factory through `factory_name`, establishing that those two physical
rows belong together.

It still does not establish whether the composite effect row designates the
factory, the system instance, or another runtime abstraction. The other 267
effect identities have neither exact same-package row. No first-match, kind
preference, or ordinal heuristic is introduced.

`shar.world-model-props.v14` now preserves authored effect relationships on the
matched primary world composite without resolving particle resources. Across
the 425 matched model composites, 340 composites carry 468 effect rows: 466
sort values are 0.5 and two are 0.1. Each row retains its authored effect index,
identity, skeleton joint, binary translucency, and optional source-width `f32`
sort value in exact composite effect order.

This v14 evidence is provenance-only. It does not select a particle-system
factory or particle-system instance, and it does not change model routing,
semantic deduplication, or runtime rendering behavior. The broader terrain-world
corpus contains 650 effect rows across 496 composites, but only the 468 rows on
matched model composites belong to the current world-model catalog boundary.

A follow-up matched-world resource audit keeps particle ownership fail-closed.
Of the 468 retained effect rows, 248 have exactly one same-package
`particle_system_factory` and one same-package `particle_system`; every such
system names the same effect identity through `factory_name`. Of the remaining
220 rows, 216 have no same-name factory/system anywhere in the normalized
terrain-world corpus, two have one same-level pair, and two face seven
same-level duplicate pairs.

The separate 63 `srr_inst_particle_system` rows do not resolve that gap. Their
normalized payloads expose particle type, maximum instances, and opaque child
chunk summaries but no authored identity that can be joined to a composite
effect row. The catalog therefore does not choose package-shared or duplicate
particle resources from those records.

`shar.world-model-props.v15` now retains the exact package-local physical
candidate pair when an authored effect has one unique same-name factory and one
unique same-name particle system. The system must identify that same logical
effect through `factory_name`, and both normalized rows must resolve to exact
phase-three `particle` / `p3d-particle` members by path and source ordinal.

Current matched-world evidence yields 248 such effect pairs, retaining 496
classified physical member references across 480 unique phase-three member IDs.
The catalog records both factory and system occurrences as candidate provenance;
it does not claim that the composite effect semantically designates either one.
The other 220 matched-world effects retain no physical candidate pair, including
all package-shared, duplicate, and absent cases described above.

A raw-schema follow-up identifies a recovery gap inside the 63 normalized
`srr_inst_particle_system` rows. Every current row reports exactly one direct
`0x00015800` `SYSTEM_FACTORY` child followed later by one direct `0x00015801`
`SYSTEM` child, with valid summarized chunk bounds. The repository P3D domain
already classifies those IDs as `particle_system_factory` and `particle_system`.

The `tlInstParticleSystemChunk` schema currently lists the factory among its
allowed subchunks but does not list the observed `SYSTEM` child. Its extractor
also reduces every nested child to ID and size metadata, even though the
existing top-level particle decoders can recover factory name/version/timing and
system name/version/`factory_name` from those chunk headers. The normalized
pipeline cache does not retain the raw P3D package bytes, so those nested names
cannot be reconstructed after extraction from current artifacts alone.

The extractor now performs that source recovery while lawful package bytes are
in scope. For each instanced-particle child stream it validates exact chunk
bounds and authored order, preserves unrelated children as opaque size evidence,
and decodes `0x00015800` factory headers into version, identity, frame rate,
animation/overlap frame counts, cycle flag, sorting flag, and emitter count. It
decodes each observed header-only `0x00015801` system into version, identity,
and `factory_name`; malformed typed child headers fail closed.

A complete synthetic P3D parser regression proves those factory/system chunks
remain nested under the `srr_inst_particle_system` parent rather than becoming
separate normalized components, while their typed identities survive inside the
parent JSON in authored order. The generated schema16 mirror is intentionally
left unchanged: it still omits the observed system child, because current corpus
observation does not rewrite the upstream generated schema authority.

The existing normalized terrain-world cache predates this recovery and retains
neither the nested names nor raw `source.p3d` package bytes. It therefore cannot
be upgraded in place. The next source-backed step is a lawful extraction rerun,
followed by a fresh audit of the 216 effect identities currently absent from
normalized top-level particle rows. Until that evidence is regenerated, v15
continues to leave those effects unresolved.

A lawful isolated `fbx-export-world-props` regeneration after nested recovery
provided stronger evidence without replacing the accepted extraction tree. The
source re-extraction reached all 149 terrain-world packages. All 63 recovered
instanced-particle parents map one-to-one to existing phase-three `world` /
`p3d-world-dsg` members, and every recovered factory is immediately followed by
its matching system at varying authored child positions.

Those 63 parents collapse to 12 logical nested factory/system identities. Every
factory identity equals its sibling system identity and `factory_name`, but none
of the 12 identities matches any authored composite effect identity in the
current terrain-world corpus. None also appears as a top-level normalized
factory or system. Nested recovery therefore closes a real extraction loss but
does not reduce the 216 matched-world effect identities that lack top-level
particle evidence.

The same regeneration exposed and fixed a separate world-texture blocker.
`SharedTextureAuthority` had incorrectly parsed physical `texture/*.png` ledger
paths through the JSON-component member helper. All 10,064 current normalized
texture rows use one-segment PNG payload paths, so texture occurrence ids now
come from the validated payload stem while schema-JSON member validation stays
unchanged.

A post-fix regeneration again completed extraction of all 149 terrain-world
packages, then failed during world candidate discovery with `prop source
identity is non-canonical`. The accepted normalized corpus audit does not yet
identify that exact field: component and ledger identities are canonical, and
the only broad secondary-field exception is 1,135 frame controllers with an
authored empty `animation_name`; none of those forms a same-package
composite-prop/hierarchy match in the static audit. The next step is therefore
a contextual discovery diagnostic or equivalent bounded audit, not weakening
`clean_identity` or treating empty values as identities.

The contextual world-inventory diagnostic isolated the previous regeneration
failure to `frame_controller_variant_b/MasterController.json@9714`: a non-BQG
`ANIM` sentinel with a NUL-only hierarchy target. The current terrain-world
corpus contains two such variant-B `ANIM` rows and four variant-A `TEX` rows;
all six have empty/sentinel hierarchy or animation targets, while every linked
deferred billboard controller is BQG. Deferred billboard controller discovery
now classifies authored controller type first and ignores non-BQG sentinels
without repairing their targets. BQG hierarchy and animation validation remains
strict.

A post-fix isolated regeneration again extracted all 149 terrain-world packages
and advanced past controller discovery. Its next fail-closed boundary is a
same-name shader ambiguity in `L1_TERRA`: `char_swatches_m_.json` at source
ordinal 12679 and `char_swatches_m___ordinal_13756.json` at ordinal 13756. Both
reference `char_swatches.bmp`, but their authored shader payloads differ: the
first has `2SID=0` and `SHIN=10`, while the second has `2SID=1` and `SHIN=0`.

The package extractor now also retains the exact primitive-group source chunk
ordinal without treating it as material-selection authority. A fresh isolated
149-package terrain-world regeneration proves the `char_swatches_m` consumer is
primitive-group ordinal 16091 inside its four-group mesh; the sibling primitive
groups are ordinals 16097, 16103, and 16109.

A committed replay now carries the candidate coordinates through the failure
boundary as well: the two physical shader occurrences are source ordinals 12679
and 13756. Both shader rows are depth-one, root-parented, self-contained ledger
occurrences, so container ancestry does not distinguish them. The phase-three
index also resolves the consuming mesh at source ordinal 16090 and both shader
occurrences to unique physical package members. The remaining gap is therefore
the missing primitive-group-to-physical-shader edge, not missing physical member
identity.

A whole current 149-package terrain-world audit finds 358 package-local
logical shader sets with duplicate physical occurrences. Of those, 338 sets are
referenced by mesh primitive groups and contain 716 physical shader occurrences:
43 sets are byte-equal and 295 sets contain differing payloads. Every one of the
716 occurrences has an exact phase-three package member at the same path and
source ordinal. Physical member indexing is therefore complete for this
ambiguity surface; the missing authored consumer-to-occurrence relationship
remains the blocking evidence.

The consumer side is now equally bounded. Across those 338 mesh-used duplicate
shader sets, all 1,312 consuming mesh occurrences resolve by exact mesh path and
source ordinal to classified phase-three `model` / `p3d-mesh` members, with zero
missing or classification-drifted matches. These stable model ids strengthen
non-selecting provenance only; they do not create a primitive-group-to-material
edge or choose among any duplicate shader occurrences.

The supported Pure3D primitive-group contract has no hidden physical material
coordinate to recover. `tlPrimGroupChunk` stores a logical `Shader` string,
primitive and vertex types, and declared geometry counts; its optional
`tlVertexShaderChunk` stores only a `VertexShaderName`. Mesh and skin containers
both consume that same primitive-group contract. The decoder therefore preserves
all available material-reference identity when it retains the shader string and
the package-level primitive-group source ordinal.

A direct raw-source audit now covers all 1,965 game P3Ds, including the 28 P3DZ
files through the repository decompressor. Its 19,875 `tlPrimGroupChunk` records
all declare version 0, so the decoder now rejects unobserved versions instead of
silently interpreting them through the version-0 field layout.

The same audit covers 14,518 mesh chunks and 633 skin chunks with zero
`NumPrimGroups` mismatches against their direct physical primitive-group
children. Mesh and skin recovery now validate that authored count before
publication rather than replacing inconsistent source evidence with a recount.

Primitive geometry-list declarations are exact across the same 19,875 raw
groups. Every group carries exactly one position list whose count matches
`NumVertices`. The 17,671 groups with nonzero `NumIndices` carry exactly one
matching index list, while all 2,204 zero-index groups omit that list. No
position- or index-list duplicates were observed, and fresh extraction now
rejects missing, duplicate, or contradictory geometry-list evidence.

Every observed per-vertex child list also agrees with `NumVertices`: all 19,875
UV lists, 14,771 colour lists, 5,104 ordinary-normal lists, 5,104 packed-normal
lists, 1,462 matrix-index lists, and 764 weight lists. No singleton list family
is duplicated. All raw UV lists use channel zero; tangent, binormal, and
multi-colour lists are absent from the current game corpus. Count validation
keeps schema-defined channel representation separate from downstream target
policy rather than inferring a broader runtime meaning.

The packed-normal audit also exposed a decoder defect. All 5,104 physical chunks
use the schema-declared one-byte layout: four bytes for the count followed by
exactly one byte per normal. The previous decoder widened each entry to `u32`
and could read through the end of the packed-normal child into a following
sibling. Primitive-list readers are now bounded to their own child headers and
packed normals remain exact source bytes; current FBX publication continues to
use the redundant ordinary-normal evidence rather than assigning semantics to
the packed representation.

Primitive matrix declarations are equally consistent across those 19,875 raw
primitive groups: 18,413 declare zero matrices and omit a matrix palette, while
1,462 declare a nonzero count and carry exactly one palette of that same length.
Primitive recovery now validates this relationship instead of retaining an
unchecked `NumMatrices` declaration beside contradictory palette evidence.

Primitive vertex-shader child presence is now retained independently from its
string value. Of the 19,875 raw groups, 13,904 carry an explicit schema-sized
vertex-shader child and every authored string is empty; the remaining 5,971 omit
the child. Fresh extraction no longer collapses those two source states into the
same empty string.

The raw mesh/skin auxiliary audit finds 15,149 schema-sized bounding boxes and
15,149 bounding spheres, plus 14,516 mesh render-status chunks with values only
`0` or `1`; each observed family is singleton per parent. One `comic_v.p3d`
mesh intentionally carries the same non-finite IEEE-754 payload in all box and
sphere-centre coordinates, so extraction retains exact float bits beside the
JSON-readable values rather than rejecting or erasing that source evidence.

That same source contains the only non-finite primitive float payload found in
the complete audit: one 72-vertex position list whose coordinates all use that
same NaN bit pattern. The other 8,721,915 position floats, 2,288,700 normal
floats, 5,814,754 UV floats, and 318,681 weight floats are finite. Sparse exact
position bits now preserve the exceptional 72 vertices without duplicating the
finite corpus.

Twenty skins also carry expression-offset containers that were previously
reduced to opaque child sizes. Each declares one primitive-group index and one
offset list; together they contain 1,592 finite 16-byte records consisting of a
vertex index and three authored float offsets. Fresh extraction retains the
container counts, indices, records, and exact offset bits while leaving morph
or runtime interpretation deferred.

World intersection evidence is equally regular across the complete raw corpus.
All 8,565 `INTERSECT_DSG` chunks carry exactly one bounding sphere and one
bounding box with no duplicate or unexpected children. Of those, 4,956 also
carry exactly one terrain-type child; every terrain child is version 0, every
index count is divisible by three, and `NumTypes` exactly equals the authored
triangle count. Intersect recovery now rejects missing singleton bounds,
duplicate children, unobserved terrain versions, and terrain-count drift.

The simulation collision audit covers 3,785 collision objects. Every object is
version 1, carries exactly one direct collision-volume child and one direct
attribute child, and its declared owner count exactly matches physical owner
children. `NumSubObject` does not consistently equal direct volumes, recursive
volumes, leaf primitives, object-reference counts, or other simple measured
relations, so that field remains preserved but intentionally uninterpreted
rather than being guessed into a runtime relationship.

The same audit finds 880 physics objects, all version 1, with finite authored
volume and resting-sensitivity values. Sparse joint records are source-backed:
positive `NumJoints` does not imply one physical joint record per index, but all
1,376 observed records use unique indices below the declared bound. Fourteen
zero-joint objects carry vector and inertia children inline inside the declared
header region while 415 equivalent zero-joint objects carry those children in
the ordinary child region. Recovery therefore rejects unobserved physics
versions while retaining sparse-joint and inline-child behavior without
inventing count equality or placement semantics.

Scene hierarchy recovery is now bounded by a complete raw-source audit of 1,152
scenegraphs. Every scenegraph is version 0, has exactly one root, and every one
of its 34,816 decoded node headers ends exactly at the schema fields; all 13,674
drawable nodes also carry exactly one finite sort-order child. Recovery
therefore rejects unobserved scenegraph versions, extra roots, and trailing
node-header
data rather than accepting schema drift.

The composite-drawable audit covers 1,542 containers and 9,169 bindings. Every
container has exactly one skin, prop, and effect list; every list declaration
matches its physical children, every binding header is exact, and every binding
has exactly one finite sort-order child. Missing lists, duplicate lists, missing
sort-order children, and trailing list or binding fields now fail closed instead
of becoming empty defaults or last-write-wins data.

Entity and physics evidence separates placement from object attributes. All
6,875 ordinary entities and 85 instanced entities are version 0; ordinary
entities contain one mesh, while instanced entities contain one mesh followed
by one legacy instance chunk. Across entity and physics wrappers, all 944 legacy
instance chunks are name-only and own exactly one scenegraph.

The distinct `0x03000600` records are not instances: all 3,407 match the
schema-defined object-attribute layout exactly, with a class type, physical
property ID, and authored sound string and no children. They occur under static
physics wrappers that have no placement instances as well as dynamic and
instanced wrappers.

World-sphere topology is also explicit in current source. All 15
`WORLD_SPHERE_DSG` containers are version 0 with exact headers. Their declared
mesh counts match 212 direct mesh children exactly, and their billboard counts
match 23 direct billboard-quad-group children exactly. The remaining direct
children are 30 frame controllers, 27 animations, 18 skeletons, 15 composite
drawables, 15 multi-controllers, and three lens flares, all schema-declared.
Fresh recovery retains each direct child's source ordinal, kind, and authored
name in physical source order rather than reducing the container to two counts.

Class types use 2, 3, 4, 7, 9, and 10; physical property IDs
span 0 through 62, while sounds include `nosound` and collision event names.
Those fields now remain object attributes instead of being mislabeled as
instance version, flags, and name metadata. Entity `HasAlpha` values include 0,
1, 8, and 9, so that authored field remains numeric rather than being
reinterpreted as a boolean.

A separate audit of the 338 mesh-used duplicate logical shader sets covers 1,312
primitive-group consumers. Every consumer has `vertex_format=8225` and an empty
optional vertex-shader reference. Within every duplicate set, all candidate
shaders agree on `vertex_needs=33`; 66 sets differ in shader `vertex_mask`, but
the primitive group publishes no shader-mask selector, and the two L1
`char_swatches_m` candidates have the same mask. These fields therefore do not
supply source-backed occurrence authority for the current ambiguity.

That exact consumer coordinate does not identify either physical shader
occurrence. The primitive-group payload still stores only the logical
`char_swatches_m` token, while the two differing shader candidates remain
independent top-level chunks at ordinals 12679 and 13756. Selecting the first,
nearest, or lowest-ordinal shader would therefore invent relationship authority;
world material export remains fail-closed until an exact source-backed
mesh-primitive-to-shader occurrence relationship is recovered or proven.

This umbrella task remains open for broader topology, UV, material, texture,
pivot, rig, animation, placement, and transform evidence.
