---
schema: shar.todo.v1
id: preserve-original-asset-identities-package-relationships-and-ordering
status: open
priority: P0
lane: conversion
active: false
parent: null
dependencies: []
children: []
---

# Preserve original asset identities, package relationships, and ordering

Preserve original asset identities, package relationships, and ordering.

## Acceptance criteria

- [ ] Preserve original asset identities, package relationships, and ordering.

## Dependencies

No explicit task dependency was recorded in the legacy TODO.

## Evidence

World scenegraph placement collection now preserves authored traversal order and
authored multiplicity instead of sorting placements by transform bits and
deduplicating identical matrices. The normalized P3D decoders already preserve
scenegraph child order and emit each contained instance graph once, so the extra
post-collection sorting/deduplication was not source authority. Two focused
regressions
prove equal source transforms remain two placements and a source order of 9 then
1 remains 9 then 1. The full pipeline library suite passes with 802 tests and
strict pipeline Clippy is clean.

World placement document discovery now also projects the six supported component
families through canonical component-ledger ordinals instead of family and file
name order. The exact world-level selector has 95 packages carrying 973 such
documents; 89 package traversals differ from the previous order. Only one
cross-document drawable identity is reordered in the current corpus, where
source ordinal 465 must precede ordinal 516 in a level-04 race-prop package. A
focused regression proves an earlier document in a later-scanned family remains
before a later standalone scenegraph document.

World component-ledger intake now rejects duplicate component ordinals instead
of silently accepting or deduplicating malformed rows. Mesh sources are ordered
only by their explicit source component ordinal; owner kind/name no longer
reorders them before coordinate matching and FBX assembly. Focused regressions
prove source ordinals 11 then 21 remain in that order even when owner names sort
the opposite way, and duplicate nested ordinal 2 fails closed. The complete
pipeline library now passes 804 tests with strict Clippy clean.

World export no longer infers new mesh identities from component proximity. The
old one-unit Blender grouping threshold could split one decoded source mesh into
multiple generated objects solely because its disconnected geometry was far
apart. Source mesh ownership is now preserved at every distance, while explicit
source owner and placement metadata still carries independent/breakable/
interactable roles.

Auxiliary coordinate-reference P3Ds no longer match canonical meshes by owner
alone or topology alone. A donor must have the same source owner kind/name, the
same mesh identity, and compatible topology inside the same package identity;
multiple exact donors fail closed. Focused tests prove owner-only and
topology-only references are ignored while one exact donor remains admissible.

World interaction roles now follow exact decoded container kinds rather than
mesh/name substring inference. `srr_tree_dsg` and `srr_breakable_object` remain
breakable, while dynamic/static-physics owners remain interactable even if their
mesh name contains `tree`. A focused regression preserves that distinction.

World component-ledger intake now also requires each normalized component path
to be unique in addition to its source ordinal. Two rows cannot claim one
component/member identity under different ordinals; a focused regression proves
duplicate `mesh/shared.json` ownership fails before mesh association.

Every grouped component must also resolve its declared root-owner ordinal after
the complete ledger is parsed. Orphan mesh rows no longer disappear through a
silent downstream skip; a focused regression proves missing owner ordinal 99
fails closed before package-mesh publication. Direct root-owner rows
additionally
must self-own: their `container_ordinal` must equal their own source ordinal,
matching the P3D extractor's deterministic top-level-ancestor contract. A
focused regression rejects a root ordinal 1 that attempts to claim container 2.

World-level mesh discovery also validates the extractor's exact relative path
contract: a row labeled `mesh` must resolve as `mesh/<member>.json` before that
member can be loaded from the mesh component directory. A focused regression
rejects `kind: mesh` provenance that instead names `shader/shared.json`.

Ledger intake now consumes the extractor's `parent_ordinal` ancestry evidence as
well: direct root owners must declare parent 0, while nested published
components
must declare a non-root parent. Focused regressions reject a depth-1 owner whose
parent is 2 and a depth-2 mesh that incorrectly names root parent 0. When a
published child also has its immediate parent present in the ledger, intake now
requires exactly one depth step and the same top-level container. Focused
regressions reject both a depth-3 child of a depth-1 parent and a child whose
published parent belongs to container 1 while the child claims container 2.

P3D normalized-package cache validation now also treats recovered component
paths by portable case-insensitive identity. A manifest cannot declare both
`components/mesh.json` and `COMPONENTS/MESH.json` as distinct complete
artifacts, matching the repository filesystem boundary on every host. P3D
extraction now applies the same identity before filesystem publication: only a
byte-identical nested repeat of the exact same path may reuse an already
published artifact. Different nested payloads, duplicate direct-root paths, and
case-equivalent paths fail closed instead of dropping or overwriting evidence.

World prop inventory now also rejects two distinct mesh members that decode to
the same source mesh identity instead of letting `BTreeMap` insertion silently
choose one member before composite association. This matches the existing
non-world component-map fail-closed identity rule. World component-ledger
intake now applies the same portable case-insensitive path identity used by P3D
publication and cache validation, while retaining a distinct diagnostic for an
exact duplicate path. A focused regression rejects `mesh/Shared.json` alongside
`mesh/shared.json` before either member can reach world assembly.

P3D cache
admission now requires the ancestry fields emitted by `component_line` and
validates the same root/nested relationship invariants before reusing normalized
artifacts. When an immediate published parent is present, depth and top-level
container continuity must also agree; omitted intermediate chunks remain valid.

Normalized P3D cache reuse is now bound to the current exact raw source bytes:
new package headers record `source_sha256`, and the batch exporter skips a
complete cache only when that local digest matches the current input. Replacing
a source P3D in place therefore forces regeneration instead of reusing stale
normalized evidence; no source path is added to the manifest. Cache reuse now
also binds the exact normalized `source.p3d` artifact separately through
`normalized_sha256`, computed from the prepared bytes actually written by the
extractor. Missing or corrupted normalized source evidence forces regeneration
even when the raw input and component files still match; a filesystem regression
proves both corruption and deletion are rejected.

The same normalized-artifact
check now replays the manifest's declared `byte_len` against the exact verified
`source.p3d` length, so a cache cannot remain current after editing that header
fact independently of the artifact digest. Each published primary component row
now also records the SHA-256 of the exact JSON/image bytes written beneath
`components/`. Source-aware cache reuse requires those digests to match in
addition to payload validity, so replacing one component with different valid
JSON or image bytes forces regeneration instead of silently accepting the edit.

Normalized-source verification now parses the exact verified `source.p3d` as a
P3D document and replays both parser-derived byte length and chunk count against
the manifest header. An edited `chunk_count` therefore cannot remain current
merely because the normalized artifact digest still matches. Source-aware cache
admission now also binds each published row's parser-derived provenance back to
that exact normalized document: ordinal lookup, depth, immediate parent,
top-level container, and decoded chunk kind must agree. A regression proves an
internally valid `texture` row cannot replace a source `mesh` row.

Non-world and world composite-prop selection now preserves the authored
`props` array order instead of converting references through `BTreeSet` and
sorting/deduplication normalization. Composite intake also rejects duplicate
cleaned prop
identities rather than silently collapsing repeated evidence that the current
outbound model cannot represent independently. Focused prop-catalog regressions
prove a source order of `zebra`, `alpha`, `middle` remains unchanged and padded
duplicate `shared` identities fail closed; all 84 prop-catalog tests pass.

FBX character shader resolution preserves the canonical package-member input
sequence instead of adding another alphabetical sort or silently deduplicating
file-stem identities. Duplicate shader identities fail closed. This resolution
order is not claimed as authored source order: binary FBX material slots are
separately derived from first primitive-group use as described below.

Character rigid prop meshes now reconstruct source order from preserved chunk
ordinals before aggregate assembly. The current character corpus has one package
where this is observable: `hibber_m` indexes its three prop meshes at source
ordinals 181, 170, and 192 in canonical path order, so assembly now restores
170, 181, 192 before those parts receive geometry identities.

Character animation-bank paths now use the same fail-closed source-ordinal
projection before clip loading. The current corpus contains 117 character
packages with 482 animation members, including 13 multi-animation packages; all
13 currently happen to match source-ordinal order. The explicit projection still
matters because request path order becomes clip order and the first clip selects
the active FBX animation stack.

Vehicle mesh, billboard, and animation source projection now reconstructs exact
source chunk order from preserved member ordinals instead of treating canonical
role/path/id index position as authored order. Missing or duplicate selected
ordinals and duplicate projected paths fail closed at the vehicle source
boundary.

The current car corpus has 535 mesh, 716 quad-group, and 105 animation members
with numeric source ordinals and no duplicate ordinals inside a package. Among
multi-member packages, 85 of 88 mesh sequences and all 84 quad-group sequences
differ from source-ordinal order, while all 22 animation sequences currently
coincide. Animation projection still uses the explicit ordinal because clip
order feeds FBX animation order and its first clip becomes the active stack.

Vehicle billboard partitioning now retains that projected source-chunk sequence
after validating each quad group instead of sorting the surviving paths
lexically. Appending the three explicitly selected shared-headlight groups also
keeps the local source sequence intact. All 84 multi-billboard car packages in
the current index differ between source-ordinal and lexical path order, so the
removed sort materially changed every such projected sequence. A synthetic
`z`-then-`a` regression covers the partition boundary.

The three selected common-package headlight quad groups now use their exact
`components.jsonl` source ordinals after identity selection. The current common
package publishes those selected groups at ordinals 92, 96, and 100, while the
former hardcoded selector returned them as 96, 92, 100. Supplemental rigid
bindings preserve their supplied order, so restoring the ledger sequence keeps
shared render evidence aligned with the same source-order contract as local
vehicle quad groups. A synthetic common-package regression proves the ordinal
projection independently of file-name order.

Vehicle effect-animation sidecars now retain that projected source-chunk order
instead of sorting their published paths lexically after emission. The current
car corpus has 14 packages with at least two non-skeletal effect animations; 10
of those source sequences differ from lexical sidecar order. For example,
`cCellC` preserves its ordinal 333 effect row before its ordinal 445 effect row.
A focused regression proves a source `z` then `a` sequence remains `z` then `a`
in the returned sidecar catalog.

Vehicle semantic part catalogs now retain the same part sequence used by the FBX
character writer instead of sorting their generated object names lexically after
the source groups have been separated. A conservative audit of source-ordinal
ordered rigid mesh groups covers 88 car packages and 1,284 primitive groups; 86
packages contain a distinct portable mesh-prefix inversion that guarantees the
former name sort changed catalog order. This lower bound excludes role-only and
non-rigid ordering differences.

Hidden wheel-proxy evidence now retains assembled composite part order instead
of sorting complete JSON objects before sidecar publication. One audited vehicle
binds its four source-backed wheel proxies to `w3`, `w2`, `w1`, then `w0`; the
old serialized-value sort reversed that sequence to `w0` through `w3`. A focused
regression proves the sidecar preserves the supplied part sequence while the FBX
continues to retain the same hidden proxy geometry.

Billboard quad-group decoding now preserves each authored child-quad identity
through the typed primitive-group boundary and emits it as FBX user metadata
without changing deterministic FBX object names. The normalized corpus contains
3,139 quads across 2,378 groups; 837 child names differ from their group name,
while only two groups repeat a child name internally, so the child name remains
provenance rather than a unique object key. No audited group, shader, or child
identity depends on surrounding-space trimming, and the decoder now rejects
such padding while retaining only fixed-width trailing NUL removal. Billboard
mode, depth, range, and other presentation semantics remain separate conversion
evidence and are not claimed complete by this identity fix.

Binary FBX material planning now preserves first-use order from the canonical
primitive-group sequence when deduplicating exact semantic variants. Material
object ids, object emission, and texture connections no longer inherit lexical
`BTreeMap` key order; a focused binary artifact regression proves `zebra_m`
remains before `alpha_m` when that is their first source-group use order.

Binary FBX skin-cluster planning now uses influence identities only for
membership and projects that membership back through the canonical skeleton
sequence. Cluster objects and connections therefore retain source skeleton
ordinals instead of lexical bone-name order; a focused regression proves a
`z_root` parent remains before its lexically earlier `a_child` child.

Decoded animation helper-group evidence now retains decoded source-group order
alongside keyed lookup. Unbound helper identities no longer inherit lexical map
order; the compact-channel fixture proves `Z_Helper` remains before
`A_Helper` when that is the source sequence.

Prop canonicalization now preserves the supplied source mesh and rigid-part
sequence while assigning anonymous `part-####` publication names. Content-based
sorting previously changed `z`-then-`a` source parts into `a`-then-`z` before
FBX publication and made those distinct authored sequences semantically
equivalent.
Static and rigid-animated regressions now prove canonical naming leaves the
source part sequence unchanged; all 86 focused prop-catalog tests and the 1,002
pipeline library tests pass with strict pipeline Clippy clean.

Decoded P3D mesh and skin intake now retains each authored mesh identity
separately from its publication name. The normalized corpus contains 14,297 mesh
documents and 633 skin documents; 11,217 mesh names and 550 skin names use only
fixed-width trailing NUL padding before the validated identity is retained.
Binary FBX mesh models publish that provenance as
`SHAR_P3D_SourceMeshIdentity`, and vehicle semantic part splitting carries the
same identity through its generated part names.

Prop canonicalization continues to assign anonymous `part-####` publication
names, but its semantic and visual deduplication hashes explicitly exclude the
provenance-only source mesh identity. Focused decoded-mesh, binary artifact,
canonicalization, and vehicle regressions cover retention while leaving sharing
behavior unchanged.

Decoded skeleton joints and supported PTRN clips now retain their validated
source identities independently of anonymous prop publication names. The
normalized corpus contains 22,223 named joints across 1,574 skeleton documents;
16,906 joint names carry fixed-width trailing NUL padding and none require
surrounding-whitespace repair after that padding is removed. Binary FBX limb
models publish the retained identity as `SHAR_P3D_SourceBoneIdentity`.

The same normalized corpus contains 6,002 animation documents, of which 1,605
are the PTRN skeletal family consumed by the typed FBX animation adapter. Across
all animation families, 4,703 names carry only trailing-NUL padding and none
require surrounding-whitespace repair after that padding is removed. PTRN clips
retain the validated source name and publish it on their FBX animation stack as
`SHAR_P3D_SourceAnimationIdentity`; the other 4,397 animation documents retain
their separate conversion contracts and are not claimed by this typed clip
boundary.

Prop canonicalization may still publish those joints and clips as `bone-####`
and `animation-####`, but semantic, rig, and world-animation deduplication
explicitly excludes the provenance-only identities. Focused canonicalization,
binary artifact, domain-validation, and hash-invariance regressions prove source
provenance survives publication naming without changing canonical sharing.

Decoded character and rigid-prop assembly now retains the authored skeleton to
composite relationship separately from the publication character name. The
normalized corpus contains 1,574 skeleton documents and 1,518 composite-drawable
documents, forming 727 distinct skeleton/composite identity pairs across 686
referenced skeleton identities. Five skeleton identities participate in more
than one composite identity, with `collectable_shadow` referenced by 38 distinct
composites, so flattening the aggregate to one source name would be lossy.

`CharacterSourceProvenance` therefore retains one validated skeleton identity
plus composite identities in supplied source order without sorting or
deduplicating them. Binary FBX publishes the skeleton identity on the export
root as `SHAR_P3D_SourceSkeletonIdentity` and emits each composite identity as
an ordinal `SHAR_P3D_SourceCompositeIdentity_####` property. Prop semantic
hashing
explicitly excludes this aggregate provenance, while vehicle semantic splitting
retains it across generated part names.

Composite binding `sort_order` remains a separate unresolved presentation
boundary and must not be used to reorder decoded arrays. The current corpus has
7,456 prop-binding and 633 skin-binding sort values; every value is finite and
numeric. Among 913 multi-prop composites, 144 would change sequence if sorted by
that value, while 886 contain duplicate sort values. The existing source array
order is therefore retained, and a future typed binding-metadata change must
preserve `sort_order` independently rather than reinterpret it as a total-order
key.

Static world-prop fallback selection now projects mesh members through exact
source component ordinals instead of sorting member ids lexically before
association. A focused `z` then `a` regression proves the fallback receives that
source sequence while composite-backed routes continue to use authored composite
prop order.

Prop and world identity cleanup now removes only fixed-width trailing NUL
padding and fails closed on empty, surrounding-whitespace, or control-character
source identities instead of repairing them with `trim`. An audit of 246,082
normalized `name` and `skeleton_name` fields found zero surrounding-whitespace
identities, zero non-NUL controls, and 168,657 fields with accepted trailing NUL
padding. A focused composite regression proves a leading-space `shared`
identity is rejected rather than silently becoming `shared`.

Vehicle decoded component-name intake now removes only trailing NUL padding and
fails closed on empty, surrounding-whitespace, or control-character source
identities instead of repairing them with `trim`. The current car-family ledgers
contain 6,135 component names with zero surrounding-space or control cases; a
focused regression proves a leading-space vehicle name is rejected.

FBX decoded component intake now removes only trailing NUL padding from mesh,
shader, and texture identities before validation. Audits of 14,297 mesh names,
18,148 primitive-group shader references, 13,916 shader names, and 14,073
texture references found zero nonempty surrounding-space or control cases.
Focused regressions cover mesh names, group shaders, shader names, texture
references, and package-ledger texture identities.

Decoded animation identity cleanup now rejects surrounding whitespace instead
of trimming it from clip or group names while retaining fixed-width trailing NUL
support. The corpus has 6,002 clip names and 36,164 group names with zero
nonempty surrounding-space or control cases; 811 empty groups remain separate
source evidence and are not reclassified by this gate.

Decoded skin intake now validates skeleton, joint, skin, shader, and composite
relationship identities before returning or matching them. The audited skin and
composite fields contain 37,650 identities, including 28,773 with trailing NUL
padding, and zero surrounding-space or control cases after padding removal.
Regressions cover skeleton names, joint names, and composite skeleton
references.

Vehicle shared-texture matching now makes source-key normalization fallible.
Among 1,317 car shader texture parameters, 1,135 use trailing NUL padding, one
is empty, and no nonempty reference has surrounding whitespace or controls;
case, extension, and fixed-width padding normalization remain intact.

Vehicle animation publication now requires the decoded source `name` instead of
inventing `animation` for a missing identity or trimming surrounding whitespace.
The current car corpus has 116 animation names, including 76 with trailing NUL
padding, 46 skeletal `PTRN` clips, and 70 effect sidecars; none are empty or
contain surrounding whitespace or controls after padding removal. Focused tests
cover missing, blank, padded, and leading-space identities while retaining
effect-sidecar source order.

FBX generated-package evidence now preserves each role's member sequence from
the generated index instead of sorting member ids lexically in both the adapter
and `ModelPackageEvidence` constructor. The current index has 419 FBX-eligible
rows. Lexical order differs from index order in 307 multi-model rows, 343
multi-material rows, 310 multi-texture rows, and 218 multi-animation rows;
`build_scene` consumes model member ids in this preserved sequence.

Minor-unit manifest publication now orders decoded components within each
source object by numeric `source_chunk_ordinal`, then places derived companions
without source positions after those chunks with deterministic fallback order.
The current manifest has 161,960 rows: 132,179 numeric source-chunk rows and
29,781 rows without source positions. Under the previous kind/path ordering,
1,920 source-object groups emitted numeric ordinals out of order. Package-index
construction now retains manifest member sequence instead of re-sorting by role,
path, and id.

Projecting the current 419 FBX-eligible index rows through their recorded source
ordinals changes 388 of 392 multi-model groups, 341 of 387 multi-material
groups, all 325 multi-texture groups, and 125 of 294 multi-animation groups
relative to the cached role/path/id order. This projection is audit evidence for
the ordering boundary; regenerated package outputs retain their own validation.

`AnimationRequirement` now preserves the member sequence supplied by package
evidence instead of sorting preserved animation ids lexically. A focused
regression proves `walk` then `idle` remains in that order while blank,
noncanonical, and duplicate-member validation remains unchanged.

Phase-three package intake now accepts that source-ordered member mirror instead
of requiring the retired phase-two role/path/id lexical order. Intake still
requires the complete member id sequence to match `unit_ids` and each role's
member sequence to match its role-specific id mirror, so source-order retention
does not weaken relationship validation.

Unreal import-manifest source publication now preserves that phase-three member
sequence within each package instead of sorting source rows by member id. Stable
package-id grouping remains deterministic. In the current index, 2,380 of 2,474
multi-member packages differ from lexical member-id order, so the former sort
materially changed most preserved package sequences before Unreal planning.

Pending FBX aggregate revisions now hash source identities and digests in that
preserved package-member order instead of sorting the source set by id first.
The current index has 715 FBX-planned packages containing 113,822 source
members; all 715 are multi-member and all 715 differ from lexical member-id
order. A
focused regression proves reversing the same source identities changes the
pending FBX plan revision.

Completion-dialog participant bindings now project audio ids and paths from the
same source-ordered package-member sequence instead of sorting paths
independently. A focused two-member regression preserves `audio-z`/`z.wav`
before `audio-a`/`a.wav`; the current cached dialog corpus has 799 audio-bearing
packages and 14,981 audio members, including 424 multi-audio packages, so this
parallel relationship remains explicit even where the cached paths happen to be
lexically ordered already.

Decoded mission locator, DynamicZone, camera, and multi-controller names now
remove only trailing fixed-width NUL padding and reject surrounding whitespace
rather than admitting a repaired semantic identity. The current corpus covers
9,654 names across those families; 6,756 carry trailing NUL padding, while zero
are empty, surrounding-whitespace padded, or control-bearing after padding
removal.

Parsed mission traffic-model, pedestrian-model, and level-player-vehicle
locator tokens now reject surrounding whitespace rather than trimming semantic
arguments a second time after mission-script parsing. The current extracted
mission command corpus contains 64 traffic model tokens, 437 pedestrian model
tokens, and 32 level-player locator tokens; all 533 are non-empty and have zero
surrounding-whitespace or control-bearing cases.

World structural-guide assembly now concatenates the accumulated
`MasterContent` mesh vector directly instead of sorting it by mesh name before
flattening geometry. A focused regression supplies `z` then `a` meshes with
distinct source positions and proves the first published triangle still comes
from `z`; the full pipeline library now passes 1,003 tests with strict pipeline
Clippy clean.

World FBX publication now preserves the accumulated source mesh sequence through
the binary writer instead of sorting by mesh name immediately before export. A
focused `z` then `a` regression proves `write_content_fbx` leaves that sequence
unchanged. Interior fusion now also keeps the exact positional pairing captured
between render meshes and their cloned ownership geometry, retaining the
fail-closed identity check without sorting both lists. Focused regressions cover
both boundaries; the full pipeline library now passes 1,005 tests with strict
pipeline Clippy clean.

The exact world-level input selector currently finds 129 extracted packages
with component ledgers. Of 123 multi-mesh packages, 120 have source-ordinal mesh
order that differs from lexical mesh-name order, covering 9,765 source meshes
across those multi-mesh packages. This is input-order evidence only; downstream
filtering and reviewed algorithms retain their separate contracts.

Prop animation canonicalization now retains decoded helper-group identities that
are intentionally unbound to deforming skeleton bones. The decoder already
preserves those validated identities in source-group order; canonicalization no
longer clears them before `PreparedGeometry` enters semantic signature hashing.
A focused regression proves `Z_Helper` then `A_Helper` remains unchanged while
exportable clip and bone identities are canonicalized; the full pipeline library
now passes 1,006 tests.

Compatible world-prop variant merging now uses animation hashes only to detect
exact duplicate clips. It retains the canonical clip vector in supplied order
and appends distinct later clips in deterministic variant order instead of
sorting the complete result by hash before publication. A focused regression
constructs a canonical two-clip sequence whose hash order is reversed and proves
an empty merge leaves that sequence unchanged.

Package-member mirrors now preserve the exact source chunk ordinal already
carried by the normalized minor-unit manifest instead of dropping it between
phase-two classification and phase-three intake. The current mirror accepts a
positive decimal ordinal or explicit `none`; historical six-field mirrors remain
readable as unavailable ordinal provenance, while malformed and zero current
ordinals fail closed. The normalized corpus has 132,179 chunk-backed indexed
members, and all 132,179 carry numeric source ordinals; 29,781 loose members
carry `none`, with no missing or invalid manifest joins.

This ordinal makes same-name package-local billboard occurrences independently
addressable without promoting filenames or names to relationship authority. In
`l5r3`, the duplicated light-pool and light-beam rows resolve exactly as source
ordinals 5311/5315 under container 5299 and 5453/5457 under container 5441 when
rejoined to the package component ledger. `container_ordinal` remains canonical
component-ledger evidence and is not yet claimed as a direct package-member
mirror.

A full current-index join confirms that omission does not create a demonstrated
preservation gap: all 132,179 chunk-backed package members resolve by package
root plus source chunk ordinal to exactly one canonical component-ledger row,
with zero lookup failures. The corpus includes 1,506 packages spanning multiple
container ordinals, so ownership remains material evidence rather than a
package-wide constant. Reviewed consumers that need source sequence project the
member ordinal explicitly, while world and Scrooby consumers that need ancestry
read the authoritative component ledger; no current relationship consumer was
found that depends on a duplicated `container_ordinal` member-mirror field.

The package-member vector itself remains the canonical phase-two role/path/id
order rather than claiming authored chunk order. A corpus audit finds 1,956
packages with at least two chunk-backed members, and 1,919 of those sequences
differ from ascending source chunk ordinal. Consumers that require authored
ordering must therefore project the preserved ordinal explicitly at a reviewed
boundary instead of treating canonical index position as source order.

Scrooby layout publication now validates source component relationships before
constructing parent and sibling maps. Duplicate component ordinals, malformed
`parent_ordinal` values, and non-root parents that do not resolve to a published
component all fail closed instead of becoming last-write-wins or null runtime
ownership. The current normalized corpus has 26 Scrooby ledgers with 13,309
Scrooby rows: all ordinals are unique, all 13,283 non-root parents resolve, and
all parent fields are numeric while the 26 root rows retain parent ordinal 0.

Scrooby binding preflight now independently requires unique source ordinals and
portable case-insensitive component-path identities before project, resource,
and joined-sprite relationships are resolved. The current normalized corpus has
26 Scrooby ledgers with 13,309 Scrooby rows, with no duplicate ordinals, exact
paths, or portable path aliases.

Selected non-instanced rigid-prop assembly now rejects repeated composite prop
bindings only when the repeated identity belongs to a mesh selected into that
asset. A later unrelated duplicate remains valid, so the selected path no longer
uses last-write-wins while general authored prop multiplicity remains available
to the instanced route. The audited Wrench and Wasp-camera selected composites
contain no repeated selected identities.

FBX texture staging now treats the portable output file name as one physical
identity. When a staged name already exists, a byte-identical source may reuse
it, but a different payload fails closed without replacing the first texture. A
focused regression proves two independent `shared.png` sources cannot silently
turn the later payload into the material source for both bindings.

Semantic-character extra textures now reserve the generated `body-atlas.png`,
`eye.png`, `eye-pupil.png`, and `eye-lids.png` identities using the same ASCII
case-folded portable comparison. Differently-cased extra names that alias to one
portable output also fail before artifact reads. Exact repeated extra names stay
valid when package assembly verifies the normalized PNG artifact is identical.

FBX local texture resolution now treats the normalized component ledger as
relationship authority before accepting a canonical direct PNG path. Duplicate
matching rows no longer collapse to one candidate, and multiple published paths
for one logical texture identity fail closed even when the direct PNG exists.
Two regressions cover exact duplicate rows and direct-path ambiguity.

A cached normalized-corpus audit covers 1,965 component ledgers and 10,064
texture rows. It finds 118 packages with 539 logical texture names mapped to
multiple published paths; 515 of those identities also have the canonical
direct PNG that previously bypassed ledger ambiguity checking. These counts are
relationship evidence only and do not claim every repeated identity is consumed
by an FBX material.

FBX local shader resolution now consults normalized component-ledger
relationships before accepting an exact or fixed-width-padded JSON member. One
logical shader identity mapping to multiple published members therefore fails
closed instead of silently selecting the direct file. A focused regression keeps
legacy padded and case-insensitive single-member lookup green.

The cached normalized corpus contains 358 case-folded logical shader identities
that map to multiple published paths. Of those sets, 306 contain differing JSON
payloads, 119 include an exact direct JSON member, and 103 combine both a direct
member and differing payloads. These counts are source-relationship evidence and
do not claim every repeated shader identity is referenced by an exported mesh.

A follow-up source-relationship audit does not support using container ordinal
as an automatic tie-breaker for repeated shader names. Across 10,745 mesh shader
references in packages containing repeated shader identities, 1,312 references
target a globally ambiguous shader name; none has a same-container shader row.
The remaining binding work therefore needs stronger decoded relationship
evidence rather than a proximity or container heuristic.

The same audit classifies the 1,312 ambiguous mesh-to-shader references by
candidate payload. 1,226 reference shader candidate sets whose normalized JSON
payloads differ, while 86 reference byte-identical candidate sets. The remaining
relationship work therefore cannot treat repeated shader names as harmless
duplicate publication in the general case.

FBX local texture lookup now compares normalized ledger names using ASCII
case-insensitive identity while retaining every matching published path for
ambiguity checks. This preserves a source reference whose extension casing
differs from its published texture evidence on case-sensitive hosts without
turning a case collision into a selected winner. A synthetic regression covers
the case-only match.

A cached corpus audit covers 14,007 decoded shader texture references. 7,954
have exact same-package ledger-name matches, two additional references have one
unambiguous case-only match, and no reference has multiple case-only matches.
The remaining 6,051 references have no same-package logical-name match and keep
their separate missing/shared-texture authority behavior.

FBX package-member mesh loading now resolves a physical normalized member path
through `components.jsonl` before validating the decoded authored mesh identity.
Collision-renamed members therefore remain independently addressable without
pretending their generated file stem is the source mesh name. Package roots
without a ledger retain the existing strict member-name fixture behavior, while
a present ledger must contain exactly one matching mesh relationship.

The cached normalized corpus contains 14,297 mesh rows. 14,276 physical member
stems equal their authored mesh identity directly, while 21 rows across 10
packages use deterministic `__ordinal_` collision suffixes. Those 21 occurrences
belong to repeated logical mesh identities with differing payloads, so
silently rejecting or coalescing the collision-renamed members would lose source
evidence.
A synthetic regression covers both ordinary package loading and
world-analysis loading through the ledger relationship.

Exact-path FBX mesh assembly now follows the same physical-versus-authored
identity boundary. Character companion meshes and selected/instanced rigid-prop
meshes use the caller-supplied physical path to select the occurrence while the
decoded mesh document retains its authored identity. They no longer compare a
deterministic `__ordinal_` collision suffix against that authored name. Across
the current corpus, all 21 collision-renamed mesh rows are unique by authored
identity within their owning container, and 16 participate in same-container
composite prop relationships.

The seven current model containers containing
those 16 occurrences route through static prop preparation and were already
protected by ledger-aware member loading; focused character and rigid-prop
regressions close the equivalent exact-path boundary without claiming a current
animated-world occurrence.

World-prop composite intake now retains authored non-mesh render relationships
in the world-prop catalog instead of silently dropping them when the selected
FBX route contains only mesh geometry. Each deferred binding preserves its
composite prop index, authored render identity, skeleton joint index, and
translucency.

Resolution prefers one same-container quad-group row, then accepts one unique
package-wide quad-group row for source-authored shared render members. The
catalog records the exact normalized member id and source component ordinal,
while ambiguous or absent component relationships remain unresolved rather than
guessed. The catalog contract advances to
`shar.world-model-props.v2` and reports an aggregate deferred-binding count.

A current normalized-corpus audit finds 425 mesh-associated world composites
across 85 packages with 260 such non-mesh bindings. Of those, 253 resolve to one
same-container quad-group occurrence and the remaining seven each resolve to one
unique package-wide quad group, with no ambiguous deferred quad-group names.
The existing billboard decoder is intentionally static inspection geometry and
does not represent billboard mode, depth/range, cutoff, or perspective
semantics, so these relationships are retained as deferred evidence rather than
flattened into an unfaithful static FBX. Focused regressions prove authored
composite order and joint/translucency survive intake, a collision-style
quad-group member retains its exact physical member and source ordinal even from
a shared package scope, and the world catalog serializes those fields without
inference.

World-prop deferred render bindings now also preserve exact source controller
relationships when one frame controller names the quad-group hierarchy. The
catalog records controller identity, physical member, source ordinal, version,
type, and frame offset plus the controller-declared animation identity. When
that animation resolves uniquely, its physical member, ordinal, version, and
type are retained as well; no animation channels are converted by this step.

The current 260 deferred world render bindings contain 138 exact controller
relationships and 122 with no matching frame controller. All 138 resolve one
controller and one animation with zero ambiguity: every controller is version 0,
type `BQG`, and frame offset 0, while every animation is version 0 and type
`BQG_`. Of the controllers, 133 share the resolved quad group's container and
five are package-shared; all 138 animations share their controller's container.
The world-prop catalog contract therefore advances to
`shar.world-model-props.v3` without claiming runtime billboard animation.

World-prop deferred billboard relationships now retain the full decoded source
presentation record in authored child order. Each resolved group preserves its
version, shader identity, depth-test/write and fog flags, while each child keeps
identity, version, billboard mode, translation, colour, UV corners, dimensions,
distance, UV offset, rotation, cutoff, range fields, and perspective flag.
Floating-point values are retained through exact `f32` bits before catalog
serialization, and unresolved logical-only relationships remain explicit.

The current 260 resolved deferred groups contain 284 authored child quads: 252
groups contain one child and eight contain four. All groups are version 0 with
depth test enabled, depth write disabled, and fog disabled; all children are
version 2. The catalog contract therefore advances to
`shar.world-model-props.v4` without treating presentation provenance as runtime
billboard behavior.

World-prop deferred billboards now retain every same-name normalized shader
occurrence rather than selecting one member by path, container, or first match.
Each occurrence keeps its physical member id, source ordinal, validated shader
fields, canonical texture token, and authored parameter order. The catalog
contract therefore advances to `shar.world-model-props.v5` while leaving the
relationship explicitly ambiguous when more than one occurrence exists.

The current 260 resolved billboards retain 271 shader occurrences. Eleven
billboards have two same-name candidates: nine candidate sets contain differing
normalized shader payloads and two are byte-equivalent. All 11 ambiguous sets
agree on their canonical `TEX` token, so texture identity does not justify
choosing between materially different shader occurrences.

The existing FBX material resolver remains fail-closed on multiple ledger
shader members for one logical identity. Deferred world provenance is different:
it records all source candidates so later native presentation work can make a
source-backed decision without losing the ambiguity itself.

The phase-three world package index and the exact `SharedTextureAuthority` scope
rules were also replayed for all 260 deferred billboard texture references.
Two hundred forty-eight resolve to one payload in the preferred scope; 12
level-03 `glow2.bmp` references remain fail-closed because the preferred
`terrain-world/level-03/terrain-mesh` package publishes two different payloads
under that logical identity.

Those conflicting texture rows are top-level source siblings at ordinals 182
and 10591. The level-03 terrain shader that references `glow2.bmp` is another
top-level row at ordinal 10648, so parent/container ownership does not select
either payload and source proximity would be an invented relationship. The 12
affected deferred billboards therefore remain unresolved rather than inheriting
first-path or nearest-ordinal behavior.

World-prop deferred billboard texture provenance now projects each canonical
shader `TEX` token through the same scoped `SharedTextureAuthority` preference
used by material staging, while retaining every preferred physical occurrence
instead of selecting one. Each occurrence keeps its owning package id and
subcategory, exact normalized texture member id, source component ordinal, and
normalized PNG SHA-256. Material resolution remains unchanged and fail-closed
when preferred payloads disagree; the world-prop catalog advances to
`shar.world-model-props.v6` only to preserve that source evidence.

Across the current 260 logical billboard texture references, v6 retains 300
preferred physical occurrences and no reference lacks preferred evidence. Two
hundred twenty-six references have one occurrence; 34 have more than one.

Of those multi-occurrence references, 22 contain only byte-identical payloads
and 12 contain differing payloads. The latter are exactly the already-audited
level-03 `glow2.bmp` conflicts. Physical duplication is therefore retained
without being mislabeled as payload ambiguity.

The 138 exact deferred billboard controller links also carry a stronger child
relationship than controller identity alone: all linked BQG animation groups
match the linked billboard child identities in exactly the same authored order.
There are 130 one-child relationships and eight four-child relationships, with
no duplicate child or animation-group identity in the linked set. World intake
now validates that exact sequence and fails closed when a controller animation
group does not match its resolved billboard child.

Those exact animation members are now decoded through a strict BQG evidence
adapter before entering world provenance. All 138 linked members pass the
adapter, retaining 162 authored groups and 1,053 authored channels while
rejecting unsupported or contradictory decoded structures instead of repairing
them.

The world catalog therefore advances to `shar.world-model-props.v7`. Controller
provenance keeps the existing physical animation member and source ordinal plus
the complete validated timing, group, channel, key, value, and interpolation
evidence; no channel is promoted to a static default or runtime relationship by
this preservation step.

A whole-corpus BQG replay also corrected the evidence shape beyond the linked
world subset. All 1,100 decoded BQG clips use one size record and one group
list; 437 rotation channels retain packed quaternion values, and visibility
retains its authored `u16` start state plus ordered raw `u16` values instead of
being misrepresented as boolean states.

The strict reader accepts 1,098 of those clips and rejects the two whose decoded
floating payload contains JSON `null` for non-finite source values. The catalog
contract advances to `shar.world-model-props.v8` for the corrected raw
visibility and packed-rotation representation; no null value is repaired or
guessed.

Vehicle effect-animation provenance now preserves more than sidecar filenames.
Across 21 audited vehicle packages, all 65 decoded BQG animation clips have
exactly one frame controller, one controller-targeted quad group, and at least
one composite reference; all 65 animation-group sequences match the target
billboard-child sequence in authored order.

`shar.vehicle-catalog.v2` therefore records each non-skeletal animation
identity, type, source ordinal, and sidecar path. BQG records additionally
retain the exact controller identity/source ordinal and billboard
identity/source ordinal. The raw effect JSON remains unchanged, so relationship
preservation neither repairs nor interprets BQG channel values.

The remaining five vehicle effect animations use source type `TEX_`. Each has
exactly one version-0 `frame_controller` with type `TEX`, finite zero frame
offset, and a unique controller hierarchy target. All five targets are shaders,
while the 65 BQG targets remain quad groups; no current effect has a missing or
ambiguous controller or target relationship.

`shar.vehicle-catalog.v3` therefore generalizes the effect-controller record
without generalizing runtime semantics. Each supported effect retains exact
controller kind, identity, source ordinal, version, type, frame-offset bits, and
the unique target component kind, identity, and source ordinal. Only the
audited `BQG_` to quad-group and `TEX_` to shader relationships are admitted;
other effect sidecars retain no invented controller relationship.

The five `TEX_` payloads now fail closed at vehicle intake when their decoded
source structure contradicts that controller-to-shader relationship. Every
current clip contains one version-0 group with id 0 and one version-0 entity
channel whose `TEX_` key count matches both frame and value counts. Key frames
are strictly increasing and in range, and all five interpolation records are
version 0, mode 1.

All five animation group identities are exactly `TEX_` plus the controller
hierarchy shader identity after fixed-width trailing-NUL removal. All 48 entity
values likewise require canonical identities after removing only trailing NUL
padding. A focused mismatch regression proves the sidecar is rejected instead
of preserving contradictory target provenance.

Physical texture occurrence selection remains intentionally unresolved for those
entity values. Of 48 current `TEX_` references, 16 have one exact package-local
texture occurrence and 32 have two exact same-name occurrences. Twenty-three of
the ambiguous pairs are byte-identical, while nine contain differing PNG
payloads. None of those 32 pairs shares the animation, controller, or target
shader container, so neither container ownership nor source proximity currently
provides relationship authority.

The target shader supplies one additional logical relationship without solving
that physical ambiguity. All five current `TEX_` target shaders declare exactly
one canonical `TEX` parameter, and each shader texture identity exactly equals
the entity-channel value keyed at source frame zero. Vehicle
intake now reads the exact target shader occurrence selected by source ordinal
through the validated shader reader and rejects a contradictory frame-zero
identity.

That check does not promote a physical texture candidate. Three of the five
frame-zero logical identities have two same-name package-local occurrences; one
pair is byte-identical and two pairs contain differing PNG payloads. The shader
and animation therefore agree on logical initial state while physical occurrence
selection remains unresolved.

`shar.vehicle-catalog.v4` now retains that unresolved physical evidence instead
of choosing a candidate. For each `TEX_` clip, logical entity identities are
recorded in authored first-use order; every exact same-package `texture` ledger
row is retained beneath the identity with its normalized physical member id,
source component ordinal, and normalized PNG SHA-256.

The five current clips produce 32 logical reference records and 48 physical
occurrence records. Sixteen references have one occurrence and 16 have two;
nine of the duplicated reference records contain differing payload digests. The
catalog therefore exposes the ambiguity without deduplicating byte-identical
peers or selecting conflicting ones.

The generated phase-three car index also contains 1,479 `p3d-texture` members
with source kind `texture`. A corrected whole-corpus audit proves these are an
exact physical mirror of the 1,479 normalized component-ledger texture rows:
every phase-three member path addresses exactly one ledger PNG, every ledger PNG
is represented once, and all 1,479 `source_chunk_ordinal` values equal the
matched ledger component ordinal.

This is expected from the generator rather than a filename heuristic.
Minor-unit provenance is read from the owning `components.jsonl` by exact
component path, then carries that same path and chunk ordinal into phase three.
The package member therefore provides a valid physical-occurrence coordinate.
It still does not establish which same-name occurrence a shader string reference
selects.

That retained occurrence evidence does not make the ambiguous material binding
exportable. All 11 quad groups that use the five `TEX_` target shaders satisfy
the strict billboard reader and are referenced by their vehicle composites, so
they enter retained material assembly. The material reader intentionally rejects
more than one package-ledger texture occurrence for one logical shader `TEX`
identity rather than choosing a member.

Three of the five frame-zero logical identities have two such occurrences. One
candidate pair is byte-identical and two pairs contain differing PNG payloads,
so those three target materials remain fail-closed before full vehicle catalog
publication. `shar.vehicle-catalog.v4` preserves the candidate relationship
model and regression evidence; it does not claim that unresolved physical
selection has become valid.

Vehicle texture authority now uses that same normalized component ledger as its
logical-to-physical source instead of reconstructing identity from PNG file
names. The 89 current car ledgers contain 1,479 texture rows and exactly 1,479
PNG members, with zero missing or orphan files, duplicate physical paths,
missing component ordinals, or duplicate component ordinals.

Filename reconstruction was observably lossy: 461 of those 1,479 rows produce a
different normalized lookup key from the authored ledger name, including 420
explicit collision-suffixed physical paths. Those occurrences are now grouped
under the authored logical identity while retaining package id, subcategory,
physical member id, component ordinal, physical path, and payload SHA-256.

This closes a fail-open cross-package lookup edge without weakening ambiguity
policy. A canonical PNG and a differing collision-renamed peer can no longer
land under separate authority keys and let the canonical member resolve alone.
The existing same-package, common-package, then global scope precedence remains
unchanged, and a selected scope still resolves only when every candidate has the
same payload digest.

The broader vehicle material corpus shows why physical selection remains open.
Of 786 currently used shader `TEX` bindings, 402 have two package-local texture
occurrences across 54 packages. Two hundred eighty-six duplicated bindings have
byte-equivalent payloads, while 116 contain different payloads. Ambiguous
material diagnostics now report exact retained occurrence and distinct-payload
counts rather than choosing one member.

A route replay bounds the immediate effect: zero currently used vehicle shader
bindings reach the `MissingTexture` cross-package fallback because each owning
material package has at least one logical texture occurrence. The active corpus
failure remains the 402 local duplicate bindings; ledger-keyed cross-package
authority hardens future/missing-texture lookup rather than claiming those local
relationships are resolved.

The 402 local duplicate bindings also do not gain an occurrence edge from chunk
hierarchy or ordering. In every case the shader and both texture rows are
top-level (`parent_ordinal = 0`) self-owned containers. Each pair has one base
path and one ordinal-qualified normalized path; both textures precede the
shader, and the later texture is closer to it in source order.

That ordering is descriptive only. The extractor creates `__ordinal_####`
strictly as a generated path-collision alias, while the canonical
`tlShaderTextureParamChunk` schema contains only a `FOURCC` parameter and string
value. It carries no texture ordinal, component handle, or parent relationship,
so choosing the nearest/later duplicate would still invent source semantics.

The FBX material adapter now exposes a separate exact texture-occurrence
boundary for consumers that later obtain real relationship authority. A caller
may supply one exact package-ledger PNG occurrence together with an exact shader
path; the physical path selects the occurrence, while the shader's authored
`TEX` identity controls the staged PNG name. Unpublished files and shaders with
no texture parameter are rejected. Ordinary material resolution is unchanged
and still rejects ambiguous same-name ledger candidates.

Community tooling evidence narrows ordering semantics without resolving that
ambiguity. Donut Team guidance for Lucas' Pure3D Editor requires a texture chunk
to appear above the shader that references it and describes shared textures as
resources found by the shader's logical texture name. All 402 current duplicate
vehicle bindings already have both same-name texture occurrences above their
shader. The public ordering evidence therefore cannot distinguish the base path
from the ordinal-qualified collision path.

`shar.vehicle-catalog.v5` now carries that verified occurrence coordinate into
TEX sidecar evidence. Every retained physical texture occurrence requires one
unique phase-three `p3d-texture` member whose exact package path and source
chunk ordinal both match the normalized ledger row. The stable package-member id
is
serialized alongside the physical member id, component ordinal, and PNG digest.

This is still non-selecting provenance. Multiple same-name occurrences remain
separate v5 records. Neither the phase-three id nor its ordinal is treated as a
shader-to-texture relationship. The new exact-occurrence FBX boundary can use
one physical member only after another source-backed consumer establishes that
relationship authority.

`shar.world-model-props.v9` now gives every retained deferred billboard
texture occurrence the same stable phase-three physical coordinate. A whole
terrain-world audit covers 149 packages and 5,866 normalized texture ledger
rows: every row
matches exactly one phase-three `p3d-texture` member by exact component path,
every member carries the same source chunk ordinal as its ledger row, and there
are zero missing, duplicate-path, or unrepresented occurrences.

World shared-texture authority now requires that exact path-and-ordinal join
while it ingests a ledger occurrence, then retains the phase-three
package-member id
alongside the existing package id, subcategory, normalized member id, source
ordinal, and PNG digest. A focused regression proves that a matching path with a
wrong ordinal, or a matching ordinal with a wrong collision path, fails closed.

This strengthens physical occurrence identity only. The existing scope
preference and payload-equivalence rules are unchanged, so the 12 differing
`glow2.bmp` preferred occurrence sets remain unresolved and all same-name
physical peers stay visible rather than being promoted to an authored shader
relationship.

`shar.world-model-props.v10` extends the same exact physical coordinate to
deferred billboard shader occurrences. A separate whole terrain-world audit
covers 9,991 normalized shader ledger rows across the same 149 packages: every
row matches exactly one phase-three `p3d-shader` member by component path and
source ordinal, with zero missing, duplicate-path, or unrepresented rows.

World inventory now builds a package-local phase-three shader map keyed only by
that exact path-and-ordinal pair. Every same-name shader occurrence retained for
a deferred billboard must resolve through the map, and v10 serializes the stable
package-member id beside the normalized shader member id and decoded authored
shader payload. The existing same-name occurrence list remains
source-ordinal ordered and non-selecting; this adds provenance without choosing
a shader winner.

The same audit method identifies the next bounded world provenance gap without
changing v10 behavior. Across the 149 terrain-world packages, all 1,091
`quad_group` rows match `p3d-mesh` phase-three members by exact path and
ordinal,
all 4,032 `animation` rows match `p3d-animation` members, and all 2,802
frame-controller-family rows match `p3d-controller` members. The controller set
contains 2,796 `frame_controller`, four `frame_controller_variant_a`, and two
`frame_controller_variant_b` rows; every family has zero missing,
duplicate-path, ordinal-mismatch, or unrepresented rows.

Those coordinates are not yet published by v10 for deferred quad-group,
controller, or animation relationships. Their current normalized member ids and
source ordinals remain truthful, so this is a stable-identity provenance gap,
not permission to infer new controller, animation, or presentation semantics.

Phase-three physical coordinates are now an intake invariant rather than an
adapter convention. A whole generated-index audit covers 161,960 current
physical members: member paths are unique within every package, and all 132,179
members that carry a source chunk ordinal also have a package-unique ordinal.
Across the 12 package categories with normalized component ledgers, all 132,179
path-bearing ledger rows map one-to-one to those same phase-three members by
exact published path plus source ordinal, with zero missing or ambiguous rows.

`PhaseThreePackageRow` now rejects duplicate physical member paths and duplicate
non-null source ordinals before any conversion adapter can observe the package.
It also exposes one exact path-and-ordinal lookup. Vehicle and world texture
occurrence joins use that shared boundary, while the world shader coordinate map
can rely on intake uniqueness instead of maintaining a second ambiguity guard.
This centralizes physical occurrence identity only; it does not turn source
ordinal into logical shader, texture, controller, or render relationship
authority.

`shar.world-model-props.v11` closes that deferred world provenance gap using
the centralized phase-three coordinate invariant. Terrain-world intake now
builds one classified source-member table for every source-ordinal package
member. Deferred composite quad groups must resolve as `p3d-mesh` model members,
frame-controller-family relationships as `p3d-controller` controller members,
and retained BQG animations as `p3d-animation` animation members at the exact
ledger path and source ordinal.

The catalog now serializes those stable phase-three ids beside the existing
normalized member ids and source ordinals. This covers the previously audited
1,091 quad groups, 2,802 frame-controller-family rows, and 4,032 animations
without changing composite selection, controller-to-animation matching, BQG
validation, or presentation semantics. A classification mismatch fails closed;
phase-three identity still records a physical occurrence rather than inventing
a logical relationship.

The next bounded world provenance gap is the primary model route rather than the
deferred render path. A route-level audit mirrors the current world inventory
across 840 model-bearing terrain-world candidates. It finds 370 rigid-animated
candidates, 55 composite-backed static candidates, and 415 static candidates
without a matching composite. Composite selection retains 2,368 mesh
occurrences in total; every selected mesh resolves to an exact phase-three
`p3d-mesh` model member at its ledger path and source ordinal.

All 425 matching composite occurrences likewise resolve as phase-three
`p3d-composite-drawable` model members. Their authored skeleton relationships
resolve to 425 exact `p3d-skeleton` animation members. Of those candidates, 370
also have the exact same-container `PTRN_<skeleton>` relationship used by the
rigid-animated route, and all 370 clips resolve as `p3d-animation` animation
members. The remaining 55 composites have the authored skeleton but no exact
PTRN clip, so the current route remains static and does not consume that
skeleton.

The world catalog source alias currently publishes none of those primary
phase-three member ids; it retains only package/container identity plus deferred
render evidence. A future catalog revision can add selected mesh member ids in
the existing authored selection order plus separately labelled
composite/skeleton/PTRN provenance without
changing semantic deduplication or implying that the 55 static skeletons were
exported. This is a physical-provenance gap only; current mesh selection and
animation routing remain source-backed.

That selected-mesh ordering must not be relabelled as numeric source-ordinal
order. Among the 425 composite-backed candidates, 233 project mesh occurrences
in an authored composite-prop order that differs from sorting those same meshes
by ledger ordinal. The remaining 415 non-composite static candidates continue to
use ledger source-ordinal order. Primary provenance must preserve whichever
source-backed ordering the existing route actually uses.

`shar.world-model-props.v12` closes the audited primary world
physical-provenance
gap without changing route selection or semantic deduplication. Every world
source alias now retains the exact phase-three package-member id, normalized
member id, and source ordinal for its owning container plus every selected mesh
occurrence. The mesh records stay in the order actually consumed by the current
route: `source_ordinal` for the 415 non-composite static candidates and
`composite_prop` for all 425 composite-backed candidates, including the 233
whose composite order differs from numeric ledger order.

Matched composite, referenced skeleton, and exported PTRN animation provenance
are represented as separate optional records. This preserves all 425 matched
composites and all 425 authored skeleton relationships, while only the 370
rigid-animated candidates carry an `exported_ptrn_animation`. The 55 static
composite-backed cases therefore retain their real skeleton provenance without
claiming that the skeleton or a nonexistent PTRN payload was exported. All 55
have a frame controller naming `PTRN_<skeleton>`, but a package-wide audit finds
no matching decoded PTRN animation payload in the same or another container, so
controller naming remains non-authoritative for route promotion.

The expected full-corpus v12 primary counts are 840 source bindings, 2,368
selected meshes, 425 matched composites, 425 referenced skeletons, and 370
exported PTRN animations. Owner occurrences are classified through the same
phase-three taxonomy as package generation: dynamic and instanced-dynamic DSG
owners are `physics/p3d-physics`, animated/state-prop factories are
`world/p3d-animated-prop`, and the remaining supported SRR model owners are
`world/p3d-world-dsg`. Classification mismatches fail closed.

The next bounded composite relationship gap is authored prop presentation rather
than physical member identity. Across the 425 matched world composites, 2,213
prop rows participate in the current source relationship surface: 1,953 select
mesh members and 260 reference non-selected render identities. All 1,953
selected rows are authored `prop` records with valid binary translucency and
joint ids from 0 through 58; 755 are translucent and 1,198 are opaque.

`sort_order` is not a corpus-wide constant. Selected mesh bindings contain
1,887 values of 0.5 and 66 values of 0.4; the 66 non-default selected rows are
translucent monkey-gag parts. The 260 non-selected prop rows contain 225 values
of 0.5, 26 of 0.4, eight of 0.49, and one of 0.0. The 0.4 non-selected cases
include chevron and power-box billboard relationships, the 0.49 cases are
`donotenter` billboard relationships, and the 0.0 case is the power-box flash
billboard relationship.

Current rigid FBX assembly consumes selected prop joint and translucency
bindings, while the pipeline's compact composite evidence retains those same
fields for deferred relationships. Both paths currently discard authored
`sort_order`, and v12 primary member provenance does not yet serialize selected
mesh joint/translucency bindings. Preserve those exact source values before any
future rendering-order interpretation; this audit does not establish what
`sort_order` means at runtime.

This umbrella task remains open for broader asset/package identity and ordering
evidence across the remaining conversion surfaces.
