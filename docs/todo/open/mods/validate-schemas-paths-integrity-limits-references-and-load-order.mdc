---
schema: shar.todo.v1
id: validate-schemas-paths-integrity-limits-references-and-load-order
status: open
priority: P4
lane: mods
active: false
parent: null
dependencies: []
children: []
---

# Validate schemas, paths, integrity, limits, references, and load order

Validate schemas, paths, integrity, limits, references, and load order.

## Acceptance criteria

- [ ] Validate schemas, paths, integrity, limits, references, and load order.

## Dependencies

No explicit task dependency was recorded in the legacy TODO.

## Evidence

Open work migrated from the canonical TODO. Add implementation and
validation evidence here as the task advances.

The shared `shar.mod-package.v1` validator now recomputes the deterministic
content revision from the canonical member set and rejects stale or arbitrary
package revision tokens. External contract tests cover both forged revision
metadata and member changes that retain an old revision.
